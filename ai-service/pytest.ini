[pytest]
pythonpath = .
testpaths = tests
timeout = 60
markers =
    manual: manual tests that require real credentials or network
    integration: tests requiring external services
    asyncio: asyncio-based tests

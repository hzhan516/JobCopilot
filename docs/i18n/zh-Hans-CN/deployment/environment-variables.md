# JobCopilot — 环境变量参考手册

> [English](../../../deployment/environment-variables.md) | [繁體中文](../../../i18n/zh-Hant-TW/deployment/environment-variables.md)

本文档描述 JobCopilot 技术栈使用的所有环境变量。变量按功能区域组织，与 `.env.example` 中的注释区块一一对应。

> **快速提示**：编辑前先执行 `cp .env.example .env`。切勿将 `.env` 提交到版本控制。
>
> 如需引导式配置，请直接用浏览器打开 `docs/deployment/env-setup.html`。该工具实时解析 `.env.example` 并生成可直接使用的 `.env` 文件，无需启动任何服务器。

---

## 目录

- [Docker Compose 配置](#docker-compose-配置)
- [A. 数据库 / PostgreSQL](#a-数据库--postgresql)
- [B. 消息队列 / RabbitMQ](#b-消息队列--rabbitmq)
- [C. 身份验证 / JWT](#c-身份验证--jwt)
- [D. 前端 / Web 应用](#d-前端--web-应用)
- [E. Spring Boot / 后端](#e-spring-boot--后端)
- [K. JWT 令牌生命周期](#k-jwt-令牌生命周期)
- [L. AI 服务连接](#l-ai-服务连接)
- [M. 文件存储](#m-文件存储)
- [N. 后端日志](#n-后端日志)
- [O. 邮箱验证配置](#o-邮箱验证配置)
- [P. 滑动验证码配置](#p-滑动验证码配置)
- [R. Redis / 共享状态层](#r-redis--共享状态层)
- [F. AI 提供商密钥](#f-ai-提供商密钥)
- [G. 模型参数](#g-模型参数)
- [H. AI 服务日志](#h-ai-服务日志)
- [I. Vertex AI 设置](#i-vertex-ai-设置)
- [J. 内部 API 密钥](#j-内部-api-密钥)

---

## Docker Compose 配置

以下值**未**在 `.env.example` 中定义，但可通过 `docker-compose.yml` 配置。它们影响容器命名和宿主机端口绑定。

### `COMPOSE_PROJECT_NAME`

| 字段 | 值 |
|------|-----|
| **用途** | 容器名称、数据卷和网络的前缀。支持在同一 Docker 宿主机上运行多个独立实例。 |
| **默认值** | 项目根目录的目录名（例如 `jobcopilot_ai_prject`） |
| **有效取值** | 任意小写字母数字字符串，可含连字符/下划线 |
| **安全说明** | 使用不同的项目名称可防止开发、预发布和生产环境之间的意外交叉污染。 |
| **常见错误** | 对同一仓库的两个克隆使用相同的项目名称，导致端口和数据卷冲突。 |

### `FRONTEND_HOST_PORT`

| 字段 | 值 |
|------|-----|
| **用途** | 映射到 Nginx 容器端口 `80` 的宿主机端口。 |
| **默认值** | `80`（通过 `docker-compose.yml` 中的 `${FRONTEND_HOST_PORT:-80}` 设置） |
| **有效取值** | 宿主机上任意空闲的 TCP 端口（`80`、`8080`、`8081`、`3000` 等） |
| **安全说明** | 生产环境中应设为 `80`（或在 TLS 终结器后设为 `443`）。请勿将后端/AI/数据库端口暴露给宿主机。 |
| **常见错误** | 在 macOS/Linux 上未使用 `sudo` 就将此值设为 `80`，因为小于 1024 的端口需要 root 权限。本地开发请使用 `8081`。 |

### `STORAGE_TYPE`

| 字段 | 值 |
|------|-----|
| **用途** | 决定上传的简历文件持久化存储位置。 |
| **默认值** | `local`（在 `docker-compose.yml` 中硬编码） |
| **有效取值** | `local`（Docker 卷）、`minio`（自托管 S3 兼容）、`s3`、`oss` |
| **安全说明** | `local` 将文件存储在命名 Docker 卷（`shared-storage`）中。对于多主机部署，请切换到 `minio` 或 `s3`，以便所有副本共享同一个对象存储。 |
| **常见错误** | 在未配置 `MINIO_ENDPOINT`、`MINIO_ACCESS_KEY` 和 `MINIO_SECRET_KEY` 的情况下将 `STORAGE_TYPE` 设为 `minio`，导致运行时文件上传失败。 |

---

## A. 数据库 / PostgreSQL

### `POSTGRES_DB`

| 字段 | 值 |
|------|-----|
| **用途** | 容器首次启动时创建的默认数据库名称。 |
| **默认值** | `JobCopilot` |
| **有效取值** | 任意有效的 PostgreSQL 标识符 |
| **安全说明** | 数据库名称不是机密，但偏离默认值可使自动化扫描稍微困难。 |
| **常见错误** | 在数据卷已初始化后重命名此变量无效。`docker-entrypoint-initdb.d` 仅在数据目录为空时首次运行。 |

### `POSTGRES_USER`

| 字段 | 值 |
|------|-----|
| **用途** | PostgreSQL 实例的超级用户名称。 |
| **默认值** | `resume_user` |
| **有效取值** | 任意有效的 PostgreSQL 用户名 |
| **安全说明** | 避免使用 `postgres` 作为用户名。生产环境中请使用具有有限权限的专用应用账户。 |
| **常见错误** | 与 `POSTGRES_DB` 相同——初始化后更改需要重建数据卷。 |

### `POSTGRES_PASSWORD`

| 字段 | 值 |
|------|-----|
| **用途** | `POSTGRES_USER` 的密码。 |
| **默认值** | `resume_pass` |
| **有效取值** | 任意字符串；建议长度 ≥ 16 个字符 |
| **安全说明** | 即使 PostgreSQL 位于隔离的 Docker 网络中，强密码仍是纵深防御的必备措施。如果容器被攻破并获得网络访问权限，弱凭据将允许轻易访问数据库。 |
| **常见错误** | 在任何非本地环境中保留默认的 `resume_pass`。 |

### `POSTGRES_HOST`

| 字段 | 值 |
|------|-----|
| **用途** | 后端连接 PostgreSQL 时使用的主机名。 |
| **默认值** | `postgres`（Docker 服务名） |
| **有效取值** | Docker 服务名、容器 IP 或外部主机名 |
| **安全说明** | 使用 Docker 服务名（`postgres`）可确保流量不会离开内部桥接网络。 |
| **常见错误** | 在容器内部将其设为 `localhost`——容器不共享主机的回环接口。 |

### `POSTGRES_PORT`

| 字段 | 值 |
|------|-----|
| **用途** | PostgreSQL 监听端口。 |
| **默认值** | `5432` |
| **有效取值** | `1024–65535` |
| **安全说明** | 非标准端口带来的安全收益极小（通过隐蔽实现安全）。网络隔离才是主要防御手段。 |
| **常见错误** | 更改此值但未同步更新 `docker-compose.yml` 中的 `ports` 映射。 |

---

## B. 消息队列 / RabbitMQ

### `RABBITMQ_HOST`

| 字段 | 值 |
|------|-----|
| **用途** | 后端和 AI 服务连接 RabbitMQ 时使用的主机名。 |
| **默认值** | `rabbitmq`（Docker 服务名） |
| **有效取值** | Docker 服务名或外部主机名 |
| **安全说明** | 内部 Docker DNS 解析使 AMQP 流量不经过宿主机网络。 |
| **常见错误** | 在容器内部使用 `localhost`。 |

### `RABBITMQ_PORT`

| 字段 | 值 |
|------|-----|
| **用途** | AMQP 协议端口。 |
| **默认值** | `5672` |
| **有效取值** | `5672`（标准）、`5671`（TLS） |
| **安全说明** | 生产环境中通过切换到端口 `5671` 并挂载 TLS 证书来启用 TLS（`amqps://`）。 |
| **常见错误** | 将其与管理面板端口（`15672`）混淆。 |

### `RABBITMQ_USERNAME`

| 字段 | 值 |
|------|-----|
| **用途** | AMQP 认证用户名。 |
| **默认值** | `guest` |
| **有效取值** | 任意字符串 |
| **安全说明** | `guest` 账户在 RabbitMQ 中默认禁用于远程连接。生产环境中务必覆盖此值。 |
| **常见错误** | 在生产部署中使用 `guest`。 |

### `RABBITMQ_PASSWORD`

| 字段 | 值 |
|------|-----|
| **用途** | AMQP 认证密码。 |
| **默认值** | `guest` |
| **有效取值** | 任意字符串；建议长度 ≥ 16 个字符 |
| **安全说明** | 这是纵深防御的第四层。即使攻击者攻破网络，仍需要有效的 MQ 凭据才能发布或消费消息。 |
| **常见错误** | 与 `POSTGRES_PASSWORD` 或 `JWT_SECRET` 使用相同的密码。请独立轮换。 |

---

## C. 身份验证 / JWT

### `JWT_SECRET`

| 字段 | 值 |
|------|-----|
| **用途** | JSON Web Token（JWT）的对称签名密钥。用于签名和验证所有用户身份验证令牌。 |
| **默认值** | `change-this-to-a-long-random-secret-at-least-32-characters` |
| **有效取值** | Base64 或纯文本字符串；**最少 32 字节（256 位）** |
| **安全说明** | 这是整个技术栈中最关键的机密。任何知晓此值的人都可以伪造身份验证令牌并冒充任意用户（包括管理员）。生产环境中请将其存储在密钥管理器中（Docker secrets、HashiCorp Vault、AWS Secrets Manager 等）。 |
| **常见错误** | <ul><li>使用简短的人类可读字符串，如 `my-secret` 或 `password123`。</li><li>将 `.env` 提交到 Git。</li><li>在开发/预发布/生产环境中使用相同的密钥。</li><li>未做规划就轮换密钥——**所有活跃用户将被迫立即重新登录**，因为其现有令牌将无法通过验证。</li></ul> |

**推荐生成方式：**

```bash
# 48 字节 = 64 个 base64 字符
openssl rand -base64 48
```

---

## D. 前端 / Web 应用

### `VITE_GOOGLE_CLIENT_ID`

| 字段 | 值 |
|------|-----|
| **用途** | 前端"使用 Google 登录"的 Google OAuth 2.0 客户端 ID。 |
| **默认值** | `your-google-oauth-client-id.apps.googleusercontent.com` |
| **有效取值** | Google Cloud Console 中有效的 OAuth 2.0 客户端 ID |
| **安全说明** | 这是公开的客户端 ID（不是密钥）。它会被嵌入到编译后的 JavaScript 包中。请在 Google Cloud Console 中将授权的 JavaScript 来源限制为您的生产域名。 |
| **常见错误** | <ul><li>本地开发时未将 `http://localhost` 添加到授权来源。</li><li>将其与客户端密钥混淆（OAuth 2.0 隐式/带 PKCE 的授权码流程不需要客户端密钥）。</li></ul> |

### `VITE_API_BASE_URL`

| 字段 | 值 |
|------|-----|
| **用途** | 所有前端 API 调用的基础 URL 前缀。 |
| **默认值** | *（空）* |
| **有效取值** | 空字符串、`/api` 或以 `/` 开头的相对路径 |
| **安全说明** | **关键警告**：此值必须为相对路径或空。当前端在 Nginx 后运行时，API 调用会自动添加前缀并代理到后端。若设置为绝对 URL（例如 `http://localhost:8080/api`），浏览器将直接连接后端，绕过 Nginx，暴露后端端口，破坏 CORS 和单入口安全模型。 |
| **常见错误** | <ul><li>本地开发时将 `VITE_API_BASE_URL` 设为 `http://localhost:8080/api`——这会将后端地址泄露给用户，且在生产环境中会失败。</li><li>将其设为内部 Docker 主机名，如 `http://backend:8080`——浏览器无法解析 Docker 服务名称。</li></ul> |

---

## E. Spring Boot / 后端

### `SPRING_PROFILES_ACTIVE`

| 字段 | 值 |
|------|-----|
| **用途** | 激活 Spring Boot 配置配置文件（Profile）。 |
| **默认值** | `dev` |
| **有效取值** | `dev`、`prod`、`test` |
| **安全说明** | `dev` 配置文件禁用 Flyway、使用 24 小时 JWT 过期时间，并启用详细错误消息。**绝不要在生产环境中使用 `dev`。** `prod` 配置文件启用 Flyway 验证、设置 1 小时 JWT 过期时间，并隐藏 API 响应中的堆栈跟踪。 |
| **常见错误** | 生产部署时仍使用 `SPRING_PROFILES_ACTIVE=dev`。 |

### `SPRING_APPLICATION_NAME`

| 字段 | 值 |
|------|-----|
| **用途** | Spring Boot 应用名称，显示在 Actuator、指标和日志中。 |
| **默认值** | `jobcopilot-backend` |
| **有效取值** | 任意有效的 Spring Boot 应用名称 |
| **安全说明** | 不是机密。用于可观测性和服务发现。 |
| **常见错误** | 使用空格或特殊字符，破坏 URL 安全标识符。 |

### `SPRING_FLYWAY_ENABLED`

| 字段 | 值 |
|------|-----|
| **用途** | 是否在启动时执行 Flyway 数据库迁移。覆盖配置文件默认值。 |
| **默认值** | `false`（dev）/ `true`（prod） |
| **有效取值** | `true`、`false` |
| **安全说明** | 在生产环境中禁用 Flyway 会阻止架构更新，当代码期望新表或新列时可能导致运行时错误。仅在手动管理架构变更时才禁用。 |
| **常见错误** | <ul><li>在生产环境中设为 `false` 却忘记手动应用迁移。</li><li>在数据库已初始化后更改此值，却不了解对现有数据的影响。</li></ul> |

---

## F. AI 提供商密钥

您只需配置**一个**提供商。选择由 `LLM_TEXT_MODEL`、`LLM_VISION_MODEL` 和 `LLM_EMBEDDING_MODEL` 中的前缀决定。

### `GEMINI_API_KEY`

| 字段 | 值 |
|------|-----|
| **用途** | Google AI Studio 的 API 密钥（通过 LiteLLM 使用 Gemini 模型）。 |
| **默认值** | `[replace-with-your-gemini-api-key]` |
| **有效取值** | [Google AI Studio](https://aistudio.google.com/app/apikey) 中有效的 Gemini API 密钥 |
| **安全说明** | Gemini 提供免费额度。免费 tier 不需要信用卡或 GCP 计费。请将此密钥视为机密——任何获得它的人都可以消耗您的额度。 |
| **常见错误** | <ul><li>将密钥粘贴到 `.env` 时带有周围空白或引号。</li><li>使用 GCP 服务账号密钥代替 AI Studio API 密钥。</li></ul> |

### `OPENAI_API_KEY`

| 字段 | 值 |
|------|-----|
| **用途** | 通过 LiteLLM 使用 OpenAI 模型（GPT-4o 等）的 API 密钥。 |
| **默认值** | *（空）* |
| **有效取值** | 以 `sk-` 开头的有效 OpenAI Service 密钥 |
| **安全说明** | OpenAI 是付费服务。请设置消费限额并监控使用仪表板，以避免意外账单。 |
| **常见错误** | 同时设置此变量和 `GEMINI_API_KEY`。LiteLLM 将使用与模型前缀匹配的提供商，但保留多个密钥会增加攻击面。 |

### `ANTHROPIC_API_KEY`

| 字段 | 值 |
|------|-----|
| **用途** | 通过 LiteLLM 使用 Anthropic Claude 模型的 API 密钥。 |
| **默认值** | *（空）* |
| **有效取值** | 有效的 Anthropic API 密钥 |
| **安全说明** | Claude 模型通常比 Gemini Flash 更昂贵。切换前请评估成本。 |
| **常见错误** | 切换提供商时忘记将模型前缀从 `gemini/` 更新为 `anthropic/`。 |

### `OLLAMA_API_BASE`

| 字段 | 值 |
|------|-----|
| **用途** | 本地 Ollama 实例的基础 URL（自托管开源模型）。 |
| **默认值** | `http://localhost:11434` |
| **有效取值** | AI 服务容器可达的任意 HTTP URL |
| **安全说明** | AI 服务容器内的 `localhost:11434` 指的是容器本身，而非 Docker 宿主机。要连接宿主机，请使用 `http://host.docker.internal:11434`（Docker Desktop）或宿主机的局域网 IP。 |
| **常见错误** | <ul><li>保留默认的 `localhost:11434`，然后疑惑 AI 服务为何无法连接。</li><li>启动技术栈前未拉取模型（`ollama pull llama3`）。</li></ul> |

---

## G. 模型参数

### `LLM_TEXT_MODEL`

| 字段 | 值 |
|------|-----|
| **用途** | 通用文本生成（职位解析、简历优化、对话）的模型标识符。 |
| **默认值** | `gemini/gemini-2.5-flash` |
| **有效取值** | 任意 LiteLLM 支持的带提供商前缀的模型字符串，例如 `gemini/gemini-2.5-flash`、`openai/gpt-4o-mini`、`vertex_ai/gemini-2.5-flash` |
| **安全说明** | 模型名称不是机密，但未经预算就切换到更昂贵的模型（例如 `gpt-4o`）可能导致意外成本。 |
| **常见错误** | 使用与配置的 API 密钥不匹配的模型前缀（例如使用 `GEMINI_API_KEY` 时却用 `openai/` 前缀）。 |

### `LLM_VISION_MODEL`

| 字段 | 值 |
|------|-----|
| **用途** | 视觉/多模态任务（简历图片解析）的模型标识符。 |
| **默认值** | `gemini/gemini-2.5-flash` |
| **有效取值** | 任意支持视觉输入的 LiteLLM 模型 |
| **安全说明** | 视觉模型通常按 token 计费更贵。请确保所选模型支持图像输入。 |
| **常见错误** | 为视觉任务选择纯文本模型（例如 `text-embedding-ada-002`）。 |

### `LLM_EMBEDDING_MODEL`

| 字段 | 值 |
|------|-----|
| **用途** | 生成文本嵌入（向量检索）的模型标识符。 |
| **默认值** | `gemini/gemini-embedding-001` |
| **有效取值** | 任意 LiteLLM 支持的嵌入模型 |
| **安全说明** | 嵌入模型的输出维度必须与 `LLM_EMBEDDING_MODEL_DIMENSION` 匹配。 |
| **常见错误** | 切换嵌入模型时未更新维度，导致 PostgreSQL 中向量插入失败。 |

### `LLM_EMBEDDING_MODEL_DIMENSION`

| 字段 | 值 |
|------|-----|
| **用途** | 所选嵌入模型的输出维度。必须与模型实际产生的向量大小匹配。 |
| **默认值** | `1536` |
| **有效取值** | 与模型输出维度匹配的正整数 |
| **安全说明** | 维度不正确会导致运行时 SQL 错误（插入 `pgvector` 列时失败），但不会造成安全漏洞。 |
| **常见错误** | <ul><li>将 `LLM_EMBEDDING_MODEL` 改为 `openai/text-embedding-3-large`（3072 维）却保留 `LLM_EMBEDDING_MODEL_DIMENSION=1536`。</li><li>更改维度后忘记重建 PostgreSQL 数据卷——现有向量列保留旧维度，将拒绝新插入。</li></ul> |

**维度对照表：**

| 模型 | 维度 |
|------|------|
| `gemini/gemini-embedding-001` | `1536` |
| `openai/text-embedding-ada-002` | `1536` |
| `openai/text-embedding-3-small` | `1536` |
| `openai/text-embedding-3-large` | `3072` |

> **注意**：`.env.example` 中的默认值 `1536` 对应当前默认的 `gemini/gemini-embedding-001` 配置以及表中列出的 OpenAI 1536 维嵌入模型。

### `LLM_TEMPERATURE`

| 字段 | 值 |
|------|-----|
| **用途** | 控制 LLM 输出的随机性（创造性）。 |
| **默认值** | `0.1` |
| **有效取值** | `0.0` 到 `2.0` |
| **安全说明** | 较低的值产生更确定的输出，对于结构化 JSON 提取（简历解析、职位匹配）更安全。较高的值增加幻觉风险。 |
| **常见错误** | 为结构化提取任务设置 `1.0` 或更高，导致无效的 JSON 响应，破坏下游解析器。 |

### `LLM_REQUEST_TIMEOUT_SECONDS`

| 字段 | 值 |
|------|-----|
| **用途** | 每次 LiteLLM 请求（文本、视觉、嵌入）的最大等待时间。 |
| **默认值** | `60` |
| **有效取值** | 正整数（秒） |
| **安全说明** | 超长的超时可能在重负载下耗尽工作线程。较短的超时提高弹性，但可能导致对慢模型的不必要的重试。 |
| **常见错误** | 对于大简历文件或批量嵌入作业，将此值设得太低（< 10 秒）。 |

### `LLM_MAX_TOKENS`

| 字段 | 值 |
|------|-----|
| **用途** | 单次 LLM 请求允许生成的最大输出 token 数。 |
| **默认值** | `8192` |
| **有效取值** | 正整数 |
| **安全说明** | 较高的值会增加单次请求成本和延迟。但设置过低会导致模型截断结构化 JSON 响应（如对话回复、简历优化），从而引发下游 `JSONDecodeError`。 |
| **常见错误** | <ul><li>使用提供商默认值（如 1024 或 2048）而未意识到它会截断长输出。</li><li>设置过高的值（> 32k）而所选模型不支持，导致提供商报错。</li><li>未将 AI 服务中的 `json.decoder.JSONDecodeError` 日志与 `max_tokens` 不足关联起来。</li></ul> |

### `BACKEND_SERVICE_URL`

| 字段 | 值 |
|------|-----|
| **用途** | AI 服务调用后端 API（例如向量 upsert）时使用的 URL。 |
| **默认值** | `http://backend:8080` |
| **有效取值** | AI 服务容器可达的任意 HTTP URL |
| **安全说明** | 使用 Docker 服务名 `backend`，使流量保持在内部网络中。 |
| **常见错误** | 在 AI 服务容器内部使用 `http://localhost:8080`。 |

### `BACKEND_QUERY_TIMEOUT`

| 字段 | 值 |
|------|-----|
| **用途** | AI 服务调用后端 API 时的超时秒数。 |
| **默认值** | `5` |
| **有效取值** | 正整数（秒） |
| **安全说明** | 较短的超时可防止 AI 服务在 backend 过载时挂起。 |
| **常见错误** | 批量向量 upsert 时将此值设得太低，导致部分数据插入。 |

### 增量模型训练参数

以下参数控制 AI worker 的增量职位训练闭环。

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `RETRAIN_INTERVAL_HOURS` | `24` | AI worker 检查缓冲反馈并重新训练的间隔时间（小时）。 |
| `MIN_SAMPLES_FOR_RETRAIN` | `10` | 触发重新训练前所需的最小缓冲反馈样本数。 |

### `AI_REDIS_KEY_PREFIX`

| 字段 | 值 |
|------|-----|
| **用途** | AI 服务共享状态（反馈缓冲、模型重训练锁）的 Redis 键前缀。允许多个 AI 服务实例共用同一 Redis 服务器而不发生键冲突。 |
| **默认值** | `ai:` |
| **有效取值** | 任意以 `:` 结尾的字符串 |
| **安全说明** | 不同环境使用不同前缀可防止多环境共用 Redis 集群时的数据交叉污染。 |
| **常见错误** | 多 AI 服务实例共用同一 Redis 服务器时未更新前缀，导致一个实例读取或覆盖另一个实例的反馈缓冲或锁状态。 |

---

## H. AI 服务日志

### `LOG_LEVEL`

| 字段 | 值 |
|------|-----|
| **用途** | 控制 AI 服务日志记录器（structlog）的详细程度。 |
| **默认值** | `INFO` |
| **有效取值** | `DEBUG`、`INFO`、`WARNING`、`ERROR`、`CRITICAL` |
| **安全说明** | `DEBUG` 可能记录包含简历内容或用户数据的请求负载。未经日志脱敏和保留策略，切勿在生产环境中使用 `DEBUG`。 |
| **常见错误** | 生产环境中保留 `DEBUG` 级别，意外将个人身份信息（PII）记录到持久化日志文件中。 |

---

## I. Vertex AI 设置

### `VERTEX_PROJECT_ID`

| 字段 | 值 |
|------|-----|
| **用途** | Vertex AI 的 Google Cloud 项目 ID（仅在使用 `vertex_ai/` 模型前缀时需要）。 |
| **默认值** | `jobcopilot-ai-service` |
| **有效取值** | 任意有效的 Google Cloud 项目 ID |
| **安全说明** | 不是机密。仅用于标识计费项目。 |
| **常见错误** | 通过 AI Studio 使用 Gemini（`gemini/` 前缀）时设置此值——该模式下此值被忽略。 |

### `VERTEX_LOCATION`

| 字段 | 值 |
|------|-----|
| **用途** | Vertex AI 模型推理的区域/位置。 |
| **默认值** | `global` |
| **有效取值** | `global`、`us-central1`、`europe-west4`、`asia-northeast1` 等 |
| **安全说明** | 选择靠近部署位置的区域以最小化延迟，并满足数据驻留合规要求。 |
| **常见错误** | 使用所选模型不可用的区域，导致 `404 Model not found` 错误。 |

### `VERTEX_CREDENTIALS`

| 字段 | 值 |
|------|-----|
| **用途** | Google Cloud 服务账号 JSON 密钥文件的宿主机绝对路径。以只读卷形式挂载到 AI 服务容器中。 |
| **默认值** | *（空）* |
| **有效取值** | 绝对文件系统路径，例如 `/home/user/service-account.json` |
| **安全说明** | **关键警告**：此值**必须**为绝对路径。相对路径或纯文件名（例如 `vertex.json`）会被 Docker/Podman 解释为命名卷引用，从而静默创建一个空卷而非挂载您的凭据文件。该文件以只读方式挂载（`:ro`）以防止意外修改。请将此密钥存储在密钥管理器中并定期轮换。 |
| **常见错误** | <ul><li>使用相对路径，如 `./vertex.json` 或 `~/keys/gcp.json`。</li><li>使用项目目录内已被 `.gitignore` 的路径，但忘记将文件复制到部署主机。</li><li>通过 AI Studio 使用 Gemini（`gemini/` 前缀）时设置此值——AI Studio 模式不需要 ADC 凭据。</li></ul> |

---

## J. 内部 API 密钥

### `INTERNAL_API_KEY`

| 字段 | 值 |
|------|-----|
| **用途** | 后端与 AI 服务 REST 端点之间的共享密钥。保护嵌入生成端点免受未经授权的内网访问。 |
| **默认值** | *（空）* |
| **有效取值** | 任意字符串；建议长度 ≥ 32 个字符 |
| **安全说明** | 这是**应用层纵深防御**密钥。即使攻击者攻破某个容器并获得内部 Docker 网络的访问权限，没有此密钥也无法调用 LLM 嵌入端点。后端通过 `InternalApiKeyInterceptor` 自动将其附加到每个出站 REST 请求。AI 服务中间件在请求头缺失或不正确时返回 HTTP 401。 |
| **常见错误** | <ul><li>在 `docker-compose.yml` 中为 `backend` 和 `ai-service` 设置不同的值——两个服务必须共享**完全相同的值**。</li><li>使用短且易被猜中的字符串。</li><li>生产环境中忘记设置，因为它在为空时也能正常工作（两端均跳过验证）。</li></ul> |

**推荐生成方式：**

```bash
# 32 字节 = 44 个 base64 字符
openssl rand -base64 32
```

若留空，后端拦截器和 AI 服务中间件均跳过检查。这便于本地开发，但**绝不可**用于生产环境。

## K. JWT 令牌生命周期

### `JWT_ACCESS_EXPIRATION`

| 字段 | 值 |
|------|-----|
| **用途** | Access Token 过期时间（毫秒）。控制用户多久需要重新认证。 |
| **默认值** | `86400000`（dev 24 小时）/ `3600000`（prod 1 小时） |
| **有效取值** | 正整数（毫秒） |
| **安全说明** | 较短的值通过限制被盗令牌的利用窗口来提高安全性。较长的值改善用户体验但增加风险。生产环境建议 1 小时（`3600000`）。 |
| **常见错误** | <ul><li>为方便起见设置极长的值（例如 `31536000000` = 1 年），这违背了令牌轮换的目的。</li><li>忘记 prod 配置文件默认就是 1 小时，即使设置了不同的 dev 值。</li></ul> |

### `JWT_REFRESH_EXPIRATION`

| 字段 | 值 |
|------|-----|
| **用途** | Refresh Token 过期时间（毫秒）。控制刷新令牌在多长时间内有效以获取新的 Access Token。 |
| **默认值** | `604800000`（7 天） |
| **有效取值** | 正整数（毫秒）；必须大于 `JWT_ACCESS_EXPIRATION` |
| **安全说明** | 刷新令牌按设计是长寿命的。如果刷新令牌泄露，攻击者可以在整个有效期内维持访问。高安全性部署中考虑更短的值（例如 1–3 天）。 |
| **常见错误** | 将 `JWT_REFRESH_EXPIRATION` 设置得比 `JWT_ACCESS_EXPIRATION` 还短，导致令牌到期前无法刷新。 |

---

## L. AI 服务连接

### `AI_SERVICE_BASE_URL`

| 字段 | 值 |
|------|-----|
| **用途** | 从后端视角看到的 AI 服务 REST API 基础地址。后端调用此端点进行向量生成和职位匹配。 |
| **默认值** | `http://localhost:8000`（非 Docker）/ `http://ai-service:8000`（Docker） |
| **有效取值** | 后端容器或进程可达的任意 HTTP URL |
| **安全说明** | 在容器网络内部使用 Docker 服务名解析。切勿在未认证的情况下将此端点暴露到公共互联网。 |
| **常见错误** | <ul><li>在 Docker 内运行时设置 `http://localhost:8000`——容器无法将 `localhost` 解析为主机。</li><li>试图通过 `.env` 覆盖此值，但 `docker-compose.yml` 已在 `backend` 服务的 `environment` 块中硬编码了该值。需先在 `docker-compose.yml` 中删除硬编码行。</li></ul> |

---

## M. 文件存储

### `STORAGE_TYPE`

| 字段 | 值 |
|------|-----|
| **用途** | 决定后端使用哪种存储后端保存上传的简历文件。 |
| **默认值** | `minio`（Spring Boot 默认值）/ `local`（Docker Compose 硬编码） |
| **有效取值** | `minio`、`local`、`s3`、`oss` |
| **安全说明** | `local` 将文件存储在 Docker 卷或本地文件系统中。生产环境中使用多后端副本时，请使用 `minio` 或 `s3`，以便所有实例共享同一个对象存储。 |
| **常见错误** | <ul><li>在未配置 MinIO 端点和凭证的情况下设置 `STORAGE_TYPE=minio`。</li><li>在多副本部署中使用 `local`，导致上传到一个副本的文件对其他副本不可见。</li></ul> |

### MinIO 配置（`STORAGE_TYPE=minio` 时生效）

### `MINIO_ENDPOINT`

| 字段 | 值 |
|------|-----|
| **用途** | MinIO 服务器地址。 |
| **默认值** | `http://localhost:9000`（dev）/ `http://minio:9000`（Docker） |
| **有效取值** | 任意 HTTP(S) URL |
| **安全说明** | 生产环境请使用 HTTPS。验证 TLS 证书或配置后端信任库。 |
| **常见错误** | 在 Docker 内使用 `http://localhost:9000` 而不是服务名。 |

### `MINIO_ACCESS_KEY`

| 字段 | 值 |
|------|-----|
| **用途** | MinIO 访问密钥（用户名）。 |
| **默认值** | `minioadmin` |
| **有效取值** | 任意字符串 |
| **安全说明** | 生产环境请立即从默认的 `minioadmin` 更换。 |
| **常见错误** | 在开发和生产 MinIO 实例之间重复使用相同的凭证。 |

### `MINIO_SECRET_KEY`

| 字段 | 值 |
|------|-----|
| **用途** | MinIO 秘密密钥（密码）。 |
| **默认值** | `minioadmin` |
| **有效取值** | 任意字符串；建议长度 ≥ 16 |
| **安全说明** | 存储在密钥管理器中。泄露的密钥将授予对所有存储桶的完全读写权限。 |
| **常见错误** | 将密钥提交到 Git 或在聊天消息中共享。 |

### `MINIO_BUCKET_NAME`

| 字段 | 值 |
|------|-----|
| **用途** | 用于简历文件的 MinIO 存储桶名称。 |
| **默认值** | `resumes` |
| **有效取值** | 任意有效的 S3 存储桶名称 |
| **安全说明** | 存储桶名称在 S3 中是全局唯一的，但在 MinIO 中仅是本地唯一。 |
| **常见错误** | 使用大写字母或下划线，这在与 DNS 兼容的 S3 存储桶名称中是无效的。 |

### `MINIO_CONNECT_TIMEOUT`

| 字段 | 值 |
|------|-----|
| **用途** | 连接 MinIO 的 TCP 超时时间（毫秒）。 |
| **默认值** | `5000` |
| **有效取值** | 正整数（毫秒） |
| **安全说明** | 较短的超时可防止在不可达的端点上挂起，但在慢网络上可能会失败。 |
| **常见错误** | 在高延迟网络上设置得太低（< 1000 毫秒）。 |

### `MINIO_WRITE_TIMEOUT`

| 字段 | 值 |
|------|-----|
| **用途** | 向 MinIO 写入数据的 Socket 超时时间（毫秒）。 |
| **默认值** | `60000` |
| **有效取值** | 正整数（毫秒） |
| **安全说明** | 大文件上传需要足够的超时时间。 |
| **常见错误** | 对于多 MB 的简历 PDF 上传设置得太低。 |

### `MINIO_READ_TIMEOUT`

| 字段 | 值 |
|------|-----|
| **用途** | 从 MinIO 读取数据的 Socket 超时时间（毫秒）。 |
| **默认值** | `30000` |
| **有效取值** | 正整数（毫秒） |
| **安全说明** | 下载大文件需要足够的超时时间。 |
| **常见错误** | 与写入超时相同——对大文件不足。 |

### 本地存储配置（`STORAGE_TYPE=local` 时生效）

### `LOCAL_STORAGE_BASE_PATH`

| 字段 | 值 |
|------|-----|
| **用途** | 本地文件存储的根目录。 |
| **默认值** | `./uploads`（dev）/ `/app/uploads`（Docker） |
| **有效取值** | 任意绝对或相对文件系统路径 |
| **安全说明** | 确保目录对后端进程用户可写。在 Docker 中，使用挂载卷以持久化数据。 |
| **常见错误** | 使用相对路径，导致根据工作目录不同解析到不同位置。 |

### `LOCAL_STORAGE_RESUME_PATH`

| 字段 | 值 |
|------|-----|
| **用途** | `LOCAL_STORAGE_BASE_PATH` 下存放简历文件的子目录。 |
| **默认值** | `resumes` |
| **有效取值** | 任意有效的目录名称 |
| **安全说明** | 无特殊要求。 |
| **常见错误** | 使用路径分隔符（例如 `resumes/2024`）——这由 `LOCAL_STORAGE_DATE_SUBDIR` 处理。 |

### `LOCAL_STORAGE_DATE_SUBDIR`

| 字段 | 值 |
|------|-----|
| **用途** | 是否为上传的文件创建基于日期的子目录（例如 `2025/05/06/`）。 |
| **默认值** | `true` |
| **有效取值** | `true`、`false` |
| **安全说明** | 日期子目录使通过扫描单个目录来枚举所有文件变得更加困难。 |
| **常见错误** | 设为 `false` 并将数千个文件累积在一个文件夹中，降低文件系统性能。 |

### `LOCAL_STORAGE_URL_PREFIX`

| 字段 | 值 |
|------|-----|
| **用途** | 生成文件访问 URL 时添加的 URL 前缀。适用于通过 Nginx 反向代理提供文件服务。 |
| **默认值** | *（空）* |
| **有效取值** | 任意 URL 路径前缀，例如 `/files`、`https://cdn.example.com` |
| **安全说明** | 如果为空，后端生成相对 URL。设置外部 CDN 可提高性能，但需要适当的访问控制。 |
| **常见错误** | 添加尾部斜杠导致 URL 中出现双斜杠。 |

### AI 模型产物存储 (MinIO)

这些变量由 Python AI 服务（AI Service 和 AI Worker）使用，用于保存和加载增量匹配模型 artifact。它们与上方后端简历文件存储设置是分开的。

### `MINIO_ENDPOINT`

| 字段 | 值 |
|------|-----|
| **用途** | MinIO 服务器地址。 |
| **默认值** | `http://minio:9000` |
| **有效取值** | 任意 HTTP(S) URL |
| **安全说明** | 生产环境请使用 HTTPS。验证 TLS 证书或配置后端信任库。 |
| **常见错误** | 在 Docker 内使用 `http://localhost:9000` 而不是服务名。 |

### `MINIO_ACCESS_KEY`

| 字段 | 值 |
|------|-----|
| **用途** | MinIO 访问密钥（用户名）。 |
| **默认值** | `minioadmin` |
| **有效取值** | 任意字符串 |
| **安全说明** | 生产环境请立即从默认的 `minioadmin` 更换。 |
| **常见错误** | 在开发和生产 MinIO 实例之间重复使用相同的凭证。 |

### `MINIO_SECRET_KEY`

| 字段 | 值 |
|------|-----|
| **用途** | MinIO 秘密密钥（密码）。 |
| **默认值** | `minioadmin` |
| **有效取值** | 任意字符串；建议长度 ≥ 16 |
| **安全说明** | 存储在密钥管理器中。泄露的密钥将授予对所有存储桶的完全读写权限。 |
| **常见错误** | 将密钥提交到 Git 或在聊天消息中共享。 |

### `MINIO_MODEL_BUCKET`

| 字段 | 值 |
|------|-----|
| **用途** | 用于保存 `ranker_model_<version>.txt` 和 `latest_meta.json` 等模型 artifact 的 bucket 名称。 |
| **默认值** | `ai-models` |
| **有效取值** | 任意有效的 S3 bucket 名称 |
| **安全说明** | 模型 artifact 可能编码聚合后的用户/职位匹配行为，应保持私有。 |
| **常见错误** | 以为 `AWS_S3_*` 变量会控制模型 artifact 存储。目前 AI 服务通过 `MINIO_*` 端点/凭证变量配置 S3-compatible 存储路径。 |

### AWS S3 配置（`STORAGE_TYPE=s3` 时生效）

### `AWS_S3_REGION`

| 字段 | 值 |
|------|-----|
| **用途** | S3 操作的 AWS 区域。 |
| **默认值** | `us-east-1` |
| **有效取值** | 任意有效的 AWS 区域代码 |
| **安全说明** | 选择靠近部署位置的区域以最小化延迟并满足数据驻留合规要求。 |
| **常见错误** | 使用存储桶不存在的区域，导致 `NoSuchBucket` 错误。 |

### `AWS_S3_ENDPOINT`

| 字段 | 值 |
|------|-----|
| **用途** | 兼容 S3 的第三方存储自定义端点（MinIO、Ceph、Wasabi 等）。 |
| **默认值** | *（空）* |
| **有效取值** | 任意 HTTP(S) URL 或空 |
| **安全说明** | 对于官方 AWS S3 请留空。第三方 S3 兼容服务需要设置此项。 |
| **常见错误** | 在真正的 AWS S3 上设置此项，覆盖官方端点并导致签名错误。 |

### `AWS_S3_ACCESS_KEY`

| 字段 | 值 |
|------|-----|
| **用途** | AWS IAM 访问密钥或 S3 兼容访问密钥。 |
| **默认值** | *（空）* |
| **有效取值** | 任意字符串 |
| **安全说明** | 在 AWS EC2/EKS 上运行时，优先使用 IAM 角色（实例配置文件）而非长期访问密钥。 |
| **常见错误** | 使用根账户密钥而非具有有限权限的 IAM 用户密钥。 |

### `AWS_S3_SECRET_KEY`

| 字段 | 值 |
|------|-----|
| **用途** | AWS IAM 秘密密钥或 S3 兼容秘密密钥。 |
| **默认值** | *（空）* |
| **有效取值** | 任意字符串 |
| **安全说明** | 与访问密钥相同——优先使用 IAM 角色。切勿提交到版本控制。 |
| **常见错误** | 粘贴密钥时带有周围空白或引号。 |

### `AWS_S3_BUCKET_NAME`

| 字段 | 值 |
|------|-----|
| **用途** | 用于简历文件的 S3 存储桶名称。 |
| **默认值** | *（空）* |
| **有效取值** | 任意有效的 S3 存储桶名称 |
| **安全说明** | 确保存储桶策略限制公共访问。启用服务器端加密。 |
| **常见错误** | 使用在全局 S3 命名空间中已被占用的存储桶名称。 |

### `AWS_S3_PATH_STYLE`

| 字段 | 值 |
|------|-----|
| **用途** | 是否使用路径风格存储桶 URL（`s3.example.com/bucketname`）而非虚拟托管风格（`bucketname.s3.example.com`）。 |
| **默认值** | `false` |
| **有效取值** | `true`、`false` |
| **安全说明** | 某些第三方 S3 兼容服务（例如不带 DNS 的 MinIO）需要路径风格。AWS S3 推荐使用虚拟托管风格。 |
| **常见错误** | 在使用基于 IP 的端点的 MinIO 上保留 `false`，导致 DNS 解析失败。 |

### `AWS_S3_CONNECTION_TIMEOUT`

| 字段 | 值 |
|------|-----|
| **用途** | S3 连接超时时间（毫秒）。 |
| **默认值** | `5000` |
| **有效取值** | 正整数（毫秒） |
| **安全说明** | 较短的超时可提高对不可达端点的恢复能力。 |
| **常见错误** | 对于高延迟区域设置得太低。 |

### `AWS_S3_SOCKET_TIMEOUT`

| 字段 | 值 |
|------|-----|
| **用途** | S3 Socket 超时时间（毫秒）。 |
| **默认值** | `50000` |
| **有效取值** | 正整数（毫秒） |
| **安全说明** | 大文件传输需要足够的超时时间。 |
| **常见错误** | 与 MinIO 读/写超时相同。 |

### `AWS_S3_MAX_CONNECTIONS`

| 字段 | 值 |
|------|-----|
| **用途** | 到 S3 的最大并发 HTTP 连接数。 |
| **默认值** | `50` |
| **有效取值** | 正整数 |
| **安全说明** | 较高的值可提高批量上传的吞吐量，但消耗更多内存。 |
| **常见错误** | 在未监控的情况下设置极高的值（> 500），导致内存压力。 |

### `AWS_S3_ENCRYPTION_ENABLED`

| 字段 | 值 |
|------|-----|
| **用途** | 是否为上传的对象启用服务器端加密。 |
| **默认值** | `false` |
| **有效取值** | `true`、`false` |
| **安全说明** | 在生产环境中启用此项以满足合规要求（GDPR、HIPAA 等）。 |
| **常见错误** | 在未指定 KMS 密钥的情况下启用加密，可能使用具有不同权限的默认 AWS 托管密钥。 |

### `AWS_S3_KMS_KEY_ID`

| 字段 | 值 |
|------|-----|
| **用途** | 服务器端加密的 AWS KMS 密钥 ID 或 ARN。仅在 `AWS_S3_ENCRYPTION_ENABLED=true` 时使用。 |
| **默认值** | *（空）* |
| **有效取值** | KMS 密钥 ID、密钥 ARN 或别名 ARN |
| **安全说明** | 使用客户托管密钥（CMK）可提供审计追踪和细粒度访问控制。 |
| **常见错误** | 指定来自不同区域的密钥，导致跨区域 KMS 调用和更高的延迟。 |

### 阿里云 OSS 配置（`STORAGE_TYPE=oss` 时生效）

### `ALIYUN_OSS_ENDPOINT`

| 字段 | 值 |
|------|-----|
| **用途** | OSS 端点 URL。 |
| **默认值** | *（空）* |
| **有效取值** | 任意有效的 OSS 端点，例如 `https://oss-cn-hangzhou.aliyuncs.com` |
| **安全说明** | 当后端和 OSS 位于同一阿里云区域时，请使用内网端点（`oss-cn-hangzhou-internal.aliyuncs.com`）以避免公网流量费用。 |
| **常见错误** | 在有内网端点可用的情况下使用公网端点，产生不必要的出站流量费用。 |

### `ALIYUN_OSS_ACCESS_KEY_ID`

| 字段 | 值 |
|------|-----|
| **用途** | 阿里云 AccessKey ID。 |
| **默认值** | *（空）* |
| **有效取值** | 任意有效的阿里云 AccessKey ID |
| **安全说明** | 在阿里云 ECS 上运行时，优先使用 RAM 角色（实例角色）而非长期 AccessKey。 |
| **常见错误** | 使用根账户 AccessKey 而非具有有限权限的 RAM 用户密钥。 |

### `ALIYUN_OSS_ACCESS_KEY_SECRET`

| 字段 | 值 |
|------|-----|
| **用途** | 阿里云 AccessKey 密码。 |
| **默认值** | *（空）* |
| **有效取值** | 任意字符串 |
| **安全说明** | 与 AccessKey ID 相同——优先使用 RAM 角色。 |
| **常见错误** | 将密钥提交到版本控制。 |

### `ALIYUN_OSS_BUCKET_NAME`

| 字段 | 值 |
|------|-----|
| **用途** | 用于简历文件的 OSS 存储桶名称。 |
| **默认值** | *（空）* |
| **有效取值** | 任意有效的 OSS 存储桶名称 |
| **安全说明** | 确保存储桶 ACL 和策略限制公共访问。 |
| **常见错误** | 使用与端点不同区域的存储桶，导致跨区域延迟。 |

### `ALIYUN_OSS_CDN_DOMAIN`

| 字段 | 值 |
|------|-----|
| **用途** | 用于生成公共文件 URL 的自定义 CDN 域名。 |
| **默认值** | *（空）* |
| **有效取值** | 任意有效域名，例如 `https://cdn.example.com` |
| **安全说明** | CDN 域名应具有适当的访问控制和 HTTPS。 |
| **常见错误** | 设置域名后忘记配置 CDN 回源设置。 |

### `ALIYUN_OSS_CONNECTION_TIMEOUT`

| 字段 | 值 |
|------|-----|
| **用途** | OSS 连接超时时间（毫秒）。 |
| **默认值** | `5000` |
| **有效取值** | 正整数（毫秒） |
| **安全说明** | 与 S3 连接超时相同。 |
| **常见错误** | 与 S3 相同。 |

### `ALIYUN_OSS_SOCKET_TIMEOUT`

| 字段 | 值 |
|------|-----|
| **用途** | OSS Socket 超时时间（毫秒）。 |
| **默认值** | `50000` |
| **有效取值** | 正整数（毫秒） |
| **安全说明** | 与 S3 Socket 超时相同。 |
| **常见错误** | 与 S3 相同。 |

### `ALIYUN_OSS_MAX_CONNECTIONS`

| 字段 | 值 |
|------|-----|
| **用途** | 到 OSS 的最大并发连接数。 |
| **默认值** | `50` |
| **有效取值** | 正整数 |
| **安全说明** | 与 S3 最大连接数相同。 |
| **常见错误** | 与 S3 相同。 |

### `ALIYUN_OSS_DOWNLOAD_BUFFER`

| 字段 | 值 |
|------|-----|
| **用途** | OSS 下载缓冲区大小（KB）。 |
| **默认值** | `64` |
| **有效取值** | 正整数（KB） |
| **安全说明** | 较大的缓冲区可提高下载速度，但增加内存使用。 |
| **常见错误** | 为小文件设置极大的值（> 1024 KB），浪费内存。 |

### `ALIYUN_OSS_UPLOAD_PART_SIZE`

| 字段 | 值 |
|------|-----|
| **用途** | 分片上传的片大小（MB）。 |
| **默认值** | `5` |
| **有效取值** | 正整数（MB） |
| **安全说明** | 大于 5 GB 的文件必须使用分片上传。较小的分片可提高断点续传能力，但增加 API 调用开销。 |
| **常见错误** | 将分片大小设置为低于 OSS 最小值（1 MB）或高于最大值（5 GB）。 |

### `ALIYUN_OSS_SSE`

| 字段 | 值 |
|------|-----|
| **用途** | OSS 对象的服务器端加密算法。 |
| **默认值** | *（空）* |
| **有效取值** | `AES256`、`KMS`、`SM4` 或空 |
| **安全说明** | 为合规启用加密。`AES256` 是最简单的选项。`KMS` 提供审计追踪。 |
| **常见错误** | 在未配置 KMS 密钥 ID 的情况下设置 `KMS`。 |

### `ALIYUN_OSS_KMS_KEY_ID`

| 字段 | 值 |
|------|-----|
| **用途** | OSS 加密的 KMS 密钥 ID。仅在 `ALIYUN_OSS_SSE=KMS` 时使用。 |
| **默认值** | *（空）* |
| **有效取值** | 任意有效的阿里云 KMS 密钥 ID |
| **安全说明** | 使用客户托管密钥比默认服务密钥提供更好的访问控制。 |
| **常见错误** | 指定来自不同区域的密钥，或没有适当的 RAM 权限。 |

---

## O. 邮箱验证配置

### `EMAIL_VERIFICATION_ENABLED`

| 字段 | 值 |
|------|-----|
| **用途** | 用户注册时邮箱验证的总开关。开启后，新用户必须输入邮件收到的6位验证码才能完成注册。 |
| **默认值** | `false` |
| **有效取值** | `true`、`false` |
| **安全说明** | 即使配置了SMTP凭据，也只有在该标志为 `true` 时才会强制验证。这确保了向后兼容和安全的渐进式上线。 |
| **常见错误** | 设为 `true` 但未配置 `SMTP_HOST`、`SMTP_USERNAME` 和 `SMTP_PASSWORD`，导致所有验证请求在运行时失败。 |

### `EMAIL_FROM`

| 字段 | 值 |
|------|-----|
| **用途** | 验证邮件中显示的发件人地址。 |
| **默认值** | `noreply@jobcopilot.local` |
| **有效取值** | 任意有效的邮箱地址 |
| **安全说明** | 某些SMTP服务商要求 `From` 地址在其控制台中已验证或已注册。使用未验证的地址可能导致邮件被拒收或进入垃圾箱。 |
| **常见错误** | 使用个人Gmail地址但未启用"应用专用密码"，或未为域名配置SPF/DKIM记录。 |

### `SMTP_HOST`

| 字段 | 值 |
|------|-----|
| **用途** | 用于发送验证邮件的SMTP中继服务器主机名。 |
| **默认值** | *（空）* |
| **有效取值** | 任何可达的SMTP主机名（例如 `smtp.gmail.com`、`smtp.office365.com`、`smtp.mailgun.org`） |
| **安全说明** | 优先选择强制使用TLS的SMTP提供商（587端口 + STARTTLS）。避免使用25端口的明文SMTP。 |
| **常见错误** | 在容器内使用 `localhost`；容器不共享主机的回环接口。如需使用本地中继，请使用Docker主机网关地址：`host.docker.internal`。 |

### `SMTP_PORT`

| 字段 | 值 |
|------|-----|
| **用途** | SMTP中继的TCP端口。 |
| **默认值** | `587` |
| **有效取值** | `25`、`465`（SSL）、`587`（STARTTLS）、`2525`（备选） |
| **安全说明** | 587端口配合STARTTLS是现代标准。465端口是遗留SSL端口。25端口通常被云服务商封锁。 |
| **常见错误** | 对STARTTLS使用465端口，或对隐式SSL使用587端口；加密方式与端口不匹配会导致连接挂起。 |

### `SMTP_USERNAME`

| 字段 | 值 |
|------|-----|
| **用途** | SMTP认证的用户名，通常完整的邮箱地址。 |
| **默认值** | *（空）* |
| **有效取值** | SMTP服务器接受的任意字符串 |
| **安全说明** | 将此视为敏感配置。不要将真实凭据提交到版本控制。 |
| **常见错误** | 对于Gmail，使用常规Google密码而非应用专用密码（开启两步验证后必须使用应用专用密码）。 |

### `SMTP_PASSWORD`

| 字段 | 值 |
|------|-----|
| **用途** | SMTP认证的密码或应用专用密码。 |
| **默认值** | *（空）* |
| **有效取值** | 任意字符串 |
| **安全说明** | 仅存储在 `.env` 中。定期轮换。如果凭据泄露，攻击者可以冒用你的身份发送邮件。 |
| **常见错误** | 将 `.env` 提交到Git。使用弱密码或重复使用的密码。团队成员离职后未轮换凭据。 |

### `EMAIL_CODE_EXPIRY`

| 字段 | 值 |
|------|-----|
| **用途** | 验证码过期前的有效时间（秒）。 |
| **默认值** | `300`（5分钟） |
| **有效取值** | 正整数，建议范围 `60–900` |
| **安全说明** | 更短的过期时间可减少暴力破解窗口。过短（< 60秒）则影响用户体验。 |
| **常见错误** | 设置过长的过期时间（例如1小时），使暴力破解攻击更容易成功。 |

### `EMAIL_RESEND_COOLDOWN`

| 字段 | 值 |
|------|-----|
| **用途** | 同一邮箱重新获取验证码前必须等待的最短秒数。 |
| **默认值** | `60`（1分钟） |
| **有效取值** | 正整数，建议范围 `30–300` |
| **安全说明** | 防止高频重发滥用。与已注册邮箱直接拒绝发件的机制配合使用。 |
| **常见错误** | 设为 `0` 会失去对误触双击和恶意滥用的防护。 |

### `EMAIL_MAX_ATTEMPTS`

| 字段 | 值 |
|------|-----|
| **用途** | 验证码校验失败的最大次数，超过则作废该验证码。 |
| **默认值** | `3` |
| **有效取值** | 正整数，建议范围 `3–5` |
| **安全说明** | 限制暴力猜测。超过阈值后，用户必须重新获取验证码。 |
| **常见错误** | 设为较大值（例如10），在5分钟有效期内使6位数字验证码更容易被暴力破解。 |

## R. Redis / 共享状态层

Redis 作为共享状态层，为分布式缓存、分布式锁（ShedLock）、去重集合和 Pub/Sub 跨实例消息提供支持。它**不替代** RabbitMQ；MQ 负责通信，Redis 负责状态。

### `REDIS_HOST`

| 字段 | 值 |
|------|-----|
| **用途** | 后端和 AI 服务连接 Redis 时使用的主机名。 |
| **默认值** | `redis`（Docker 服务名）/ `localhost`（非 Docker） |
| **有效取值** | Docker 服务名、容器 IP 或外部主机名 |
| **安全说明** | 使用 Docker 服务名（`redis`）可确保流量停留在内部网桥网络上。 |
| **常见错误** | 在容器内部使用 `localhost`。 |

### `REDIS_PORT`

| 字段 | 值 |
|------|-----|
| **用途** | Redis 监听端口。 |
| **默认值** | `6379` |
| **有效取值** | `1024–65535` |
| **安全说明** | 网络隔离是主要防御手段。更改端口带来的收益极小。 |
| **常见错误** | 更改端口后未同步更新 Redis 容器命令/配置以及后端、AI 服务环境变量。Docker Compose 默认不向宿主机暴露 Redis。 |

### `REDIS_PASSWORD`

| 字段 | 值 |
|------|-----|
| **用途** | Redis AUTH 密码。 |
| **默认值** | *（空）* |
| **有效取值** | 任意字符串；留空表示无认证（开发环境默认值） |
| **安全说明** | 生产环境中应设置强密码并配置 Redis 的 `requirepass`。即使具备网络隔离，密码认证仍是纵深防御的必备层。 |
| **常见错误** | 在生产部署中留空密码。 |

---

## P. 滑动验证码配置

### `CAPTCHA_ENABLED`

| 字段 | 值 |
|------|-----|
| **用途** | 滑动验证码总开关。开启后，注册和登录接口要求完成验证码校验。 |
| **默认值** | `true` |
| **有效取值** | `true`、`false` |
| **安全说明** | 关闭验证码会移除一层机器人防护。仅在受信任的内部网络或自动化测试环境中设为 `false`。 |
| **常见错误** | 在面向公众的实例上设为 `false`，且未配置替代防机器人措施（例如速率限制或 WAF 规则）。 |

### `CAPTCHA_TOLERANCE`

| 字段 | 值 |
|------|-----|
| **用途** | 校验滑块位置时的像素容差。数值越大，拼图越容易通过。 |
| **默认值** | `8` |
| **有效取值** | 非负整数，建议范围 `4–20` |
| **安全说明** | 容差过大（例如 `50`）会实质性地失效挑战，因为任何落点都会被接受。 |
| **常见错误** | 设为 `0` 要求像素级对齐，导致正常用户反复失败并放弃注册。 |

### `CAPTCHA_TOKEN_EXPIRY`

| 字段 | 值 |
|------|-----|
| **用途** | captchaToken 过期前的有效时间（秒）。 |
| **默认值** | `300`（5分钟） |
| **有效取值** | 正整数，建议范围 `60–600` |
| **安全说明** | 更短的过期时间可减少重放攻击窗口。过短（< 30秒）则在慢速网络下影响用户体验。 |
| **常见错误** | 设置过长的过期时间（例如1小时），使攻击者可以复用一次已解决的挑战发起多次请求。 |

### `CAPTCHA_TRACK_WIDTH`

| 字段 | 值 |
|------|-----|
| **用途** | 滑轨宽度（像素）。决定挑战的难度范围。 |
| **默认值** | `300` |
| **有效取值** | 正整数，建议范围 `200–500` |
| **安全说明** | 过窄的滑轨（< 150 px）更容易被机器人暴力破解，因为搜索空间更小。 |
| **常见错误** | 设置的宽度与前端 CSS 不匹配，导致视觉错位和落点失败。 |

### `CAPTCHA_MAX_ATTEMPTS`

| 字段 | 值 |
|------|-----|
| **用途** | 单条 challenge 的最大验证次数，超过则作废该 captchaToken。 |
| **默认值** | `5` |
| **有效取值** | 正整数，建议范围 `3–10` |
| **安全说明** | 限制对滑块位置的暴力猜测。超过阈值后，用户必须重新获取挑战。 |
| **常见错误** | 设为较大值（例如50），在 token 有效期内允许无限制猜测，使挑战失去意义。 |

### `CAPTCHA_REDIS_KEY_PREFIX`

| 字段 | 值 |
|------|-----|
| **用途** | CAPTCHA challenge、token 和限流数据的 Redis 键前缀。允许多个后端实例共用同一 Redis 服务器而不发生键冲突。 |
| **默认值** | `ra:captcha:` |
| **有效取值** | 任意以 `:` 结尾的字符串 |
| **安全说明** | 修改此值不影响安全性，但不同环境使用不同前缀（如 `staging:captcha:`、`prod:captcha:`）可防止多环境共用 Redis 集群时的数据交叉污染。 |
| **常见错误** | 多后端实例共用同一 Redis 服务器时未更新前缀，导致一个实例读取或删除另一个实例的 CAPTCHA 数据。 |

---

## N. 后端日志

### `LOG_HIBERNATE_LEVEL`

| 字段 | 值 |
|------|-----|
| **用途** | Hibernate SQL 语句和 JDBC 操作的日志级别。 |
| **默认值** | `DEBUG`（dev）/ `WARN`（prod） |
| **有效取值** | `TRACE`、`DEBUG`、`INFO`、`WARN`、`ERROR`、`OFF` |
| **安全说明** | `DEBUG` 和 `TRACE` 可能记录包含简历中个人数据的绑定参数值。未经日志脱敏，切勿在生产环境中使用 `DEBUG`。 |
| **常见错误** | 在生产环境中保留 `DEBUG`，意外将个人身份信息（PII）记录到持久化文件中。 |

### `LOG_APP_LEVEL`

| 字段 | 值 |
|------|-----|
| **用途** | 应用代码（`io.jobcopilot.resumeassistant`）的日志级别。 |
| **默认值** | `DEBUG`（dev）/ `INFO`（prod） |
| **有效取值** | `TRACE`、`DEBUG`、`INFO`、`WARN`、`ERROR`、`OFF` |
| **安全说明** | `DEBUG` 会打印请求详情、服务调用和异常堆栈跟踪。对故障排除很有用，但会生成大量日志。 |
| **常见错误** | 在未配置日志轮转的情况下设置 `TRACE`，导致繁忙服务器在数小时内耗尽磁盘空间。 |

---

# Resume Assistant — 環境變數參考手冊

> [English](../../../deployment/environment-variables.md) | [简体中文](../../../i18n/zh-Hans-CN/deployment/environment-variables.md)

本文件描述 Resume Assistant 技術堆疊使用的所有環境變數。變數依功能區域組織，與 `.env.example` 中的註解區塊一一對應。

> **快速提示**：編輯前先執行 `cp .env.example .env`。切勿將 `.env` 提交到版本控制。
>
> 如需引導式配置，請直接用瀏覽器開啟 `docs/deployment/env-setup.html`。該工具即時解析 `.env.example` 並生成可直接使用的 `.env` 檔案，無需啟動任何伺服器。

---

## 目錄

- [Docker Compose 設定](#docker-compose-設定)
- [A. 資料庫 / PostgreSQL](#a-資料庫--postgresql)
- [B. 訊息佇列 / RabbitMQ](#b-訊息佇列--rabbitmq)
- [C. 身分驗證 / JWT](#c-身分驗證--jwt)
- [D. 前端 / Web 應用程式](#d-前端--web-應用程式)
- [E. Spring Boot / 後端](#e-spring-boot--後端)
- [K. JWT 令牌生命週期](#k-jwt-令牌生命週期)
- [L. AI 服務連線](#l-ai-服務連線)
- [M. 檔案儲存](#m-檔案儲存)
- [N. 後端日誌](#n-後端日誌)
- [O. 郵箱驗證設定](#o-郵箱驗證設定)
- [P. 滑動驗證碼配置](#p-滑動驗證碼配置)
- [R. Redis / 共享狀態層](#r-redis--共享狀態層)
- [F. AI 供應商金鑰](#f-ai-供應商金鑰)
- [G. 模型參數](#g-模型參數)
- [H. AI 服務日誌](#h-ai-服務日誌)
- [I. Vertex AI 設定](#i-vertex-ai-設定)
- [J. 內部 API 金鑰](#j-內部-api-金鑰)

---

## Docker Compose 設定

以下值**未**在 `.env.example` 中定義，但可透過 `docker-compose.yml` 設定。它們影響容器命名與主機連接埠繫結。

### `COMPOSE_PROJECT_NAME`

| 欄位 | 值 |
|------|-----|
| **用途** | 容器名稱、資料卷與網路的前綴。支援在同一 Docker 主機上執行多個獨立實例。 |
| **預設值** | 專案根目錄的目錄名稱（例如 `ser594_ai_prject`） |
| **有效取值** | 任意小寫字母數字字串，可含連字號/底線 |
| **安全說明** | 使用不同的專案名稱可防止開發、預備和生產環境之間的意外交叉污染。 |
| **常見錯誤** | 對同一儲存庫的兩個複本使用相同的專案名稱，導致連接埠和資料卷衝突。 |

### `FRONTEND_HOST_PORT`

| 欄位 | 值 |
|------|-----|
| **用途** | 映射到 Nginx 容器連接埠 `80` 的主機連接埠。 |
| **預設值** | `8081`（在 `docker-compose.yml.example` 中） |
| **有效取值** | 主機上任意閒置的 TCP 連接埠（`80`、`8080`、`8081`、`3000` 等） |
| **安全說明** | 生產環境中應設為 `80`（或在 TLS 終止器後設為 `443`）。請勿將後端/AI/資料庫連接埠暴露給主機。 |
| **常見錯誤** | 在 macOS/Linux 上未使用 `sudo` 就將此值設為 `80`，因為小於 1024 的連接埠需要 root 權限。本地開發請使用 `8081`。 |

### `STORAGE_TYPE`

| 欄位 | 值 |
|------|-----|
| **用途** | 決定上傳的履歷檔案持續性儲存位置。 |
| **預設值** | `local`（在 `docker-compose.yml` 中硬編碼） |
| **有效取值** | `local`（Docker 卷）、`minio`（自托管 S3 相容）、`s3`、`oss` |
| **安全說明** | `local` 將檔案儲存在命名 Docker 卷（`shared-storage`）中。對於多主機部署，請切換到 `minio` 或 `s3`，以便所有副本共享同一個物件儲存。 |
| **常見錯誤** | 在未設定 `MINIO_ENDPOINT`、`MINIO_ACCESS_KEY` 和 `MINIO_SECRET_KEY` 的情況下將 `STORAGE_TYPE` 設為 `minio`，導致執行時檔案上傳失敗。 |

---

## A. 資料庫 / PostgreSQL

### `POSTGRES_DB`

| 欄位 | 值 |
|------|-----|
| **用途** | 容器首次啟動時建立的預設資料庫名稱。 |
| **預設值** | `resume_assistant` |
| **有效取值** | 任意有效的 PostgreSQL 識別項 |
| **安全說明** | 資料庫名稱不是機密，但偏離預設值可使自動化掃描稍微困難。 |
| **常見錯誤** | 在資料卷已初始化後重新命名此變數無效。`docker-entrypoint-initdb.d` 僅在資料目錄為空時首次執行。 |

### `POSTGRES_USER`

| 欄位 | 值 |
|------|-----|
| **用途** | PostgreSQL 實例的超級使用者名稱。 |
| **預設值** | `resume_user` |
| **有效取值** | 任意有效的 PostgreSQL 使用者名稱 |
| **安全說明** | 避免使用 `postgres` 作為使用者名稱。生產環境中請使用具有有限權限的專用應用程式帳戶。 |
| **常見錯誤** | 與 `POSTGRES_DB` 相同——初始化後變更需要重建資料卷。 |

### `POSTGRES_PASSWORD`

| 欄位 | 值 |
|------|-----|
| **用途** | `POSTGRES_USER` 的密碼。 |
| **預設值** | `resume_pass` |
| **有效取值** | 任意字串；建議長度 ≥ 16 個字元 |
| **安全說明** | 即使 PostgreSQL 位於隔離的 Docker 網路中，強密碼仍是縱深防禦的必備措施。如果容器被攻破並獲得網路存取權限，弱憑證將允許輕易存取資料庫。 |
| **常見錯誤** | 在任何非本地環境中保留預設的 `resume_pass`。 |

### `POSTGRES_HOST`

| 欄位 | 值 |
|------|-----|
| **用途** | 後端連線 PostgreSQL 時使用的主機名稱。 |
| **預設值** | `postgres`（Docker 服務名稱） |
| **有效取值** | Docker 服務名稱、容器 IP 或外部主機名稱 |
| **安全說明** | 使用 Docker 服務名稱（`postgres`）可確保流量不會離開內部橋接網路。 |
| **常見錯誤** | 在容器內部將其設為 `localhost`——容器不共享主機的回環介面。 |

### `POSTGRES_PORT`

| 欄位 | 值 |
|------|-----|
| **用途** | PostgreSQL 監聽連接埠。 |
| **預設值** | `5432` |
| **有效取值** | `1024–65535` |
| **安全說明** | 非標準連接埠帶來的安全收益極小（透過隱蔽實現安全）。網路隔離才是主要防禦手段。 |
| **常見錯誤** | 變更此值但未同步更新 `docker-compose.yml` 中的 `ports` 映射。 |

---

## B. 訊息佇列 / RabbitMQ

### `RABBITMQ_HOST`

| 欄位 | 值 |
|------|-----|
| **用途** | 後端和 AI 服務連線 RabbitMQ 時使用的主機名稱。 |
| **預設值** | `rabbitmq`（Docker 服務名稱） |
| **有效取值** | Docker 服務名稱或外部主機名稱 |
| **安全說明** | 內部 Docker DNS 解析使 AMQP 流量不經過主機網路。 |
| **常見錯誤** | 在容器內部使用 `localhost`。 |

### `RABBITMQ_PORT`

| 欄位 | 值 |
|------|-----|
| **用途** | AMQP 協定連接埠。 |
| **預設值** | `5672` |
| **有效取值** | `5672`（標準）、`5671`（TLS） |
| **安全說明** | 生產環境中透過切換到連接埠 `5671` 並掛載 TLS 憑證來啟用 TLS（`amqps://`）。 |
| **常見錯誤** | 將其與管理面板連接埠（`15672`）混淆。 |

### `RABBITMQ_USERNAME`

| 欄位 | 值 |
|------|-----|
| **用途** | AMQP 認證使用者名稱。 |
| **預設值** | `guest` |
| **有效取值** | 任意字串 |
| **安全說明** | `guest` 帳戶在 RabbitMQ 中預設禁用於遠端連線。生產環境中務必覆蓋此值。 |
| **常見錯誤** | 在生產部署中使用 `guest`。 |

### `RABBITMQ_PASSWORD`

| 欄位 | 值 |
|------|-----|
| **用途** | AMQP 認證密碼。 |
| **預設值** | `guest` |
| **有效取值** | 任意字串；建議長度 ≥ 16 個字元 |
| **安全說明** | 這是縱深防禦的第四層。即使攻擊者攻破網路，仍需要有效的 MQ 憑證才能發布或消費訊息。 |
| **常見錯誤** | 與 `POSTGRES_PASSWORD` 或 `JWT_SECRET` 使用相同的密碼。請獨立輪換。 |

---

## C. 身分驗證 / JWT

### `JWT_SECRET`

| 欄位 | 值 |
|------|-----|
| **用途** | JSON Web Token（JWT）的對稱簽章金鑰。用於簽章和驗證所有使用者身分驗證令牌。 |
| **預設值** | `change-this-to-a-long-random-secret-at-least-32-characters` |
| **有效取值** | Base64 或純文字字串；**最少 32 位元組（256 位元）** |
| **安全說明** | 這是整個技術堆疊中最關鍵的機密。任何知曉此值的人都可以偽造身分驗證令牌並冒充任意使用者（包括管理員）。生產環境中請將其儲存在金鑰管理器中（Docker secrets、HashiCorp Vault、AWS Secrets Manager 等）。 |
| **常見錯誤** | <ul><li>使用簡短的人類可讀字串，如 `my-secret` 或 `password123`。</li><li>將 `.env` 提交到 Git。</li><li>在開發/預備/生產環境中使用相同的金鑰。</li><li>未做規劃就輪換金鑰——**所有活躍使用者將被迫立即重新登入**，因為其現有令牌將無法通過驗證。</li></ul> |

**推薦產生方式：**

```bash
# 48 位元組 = 64 個 base64 字元
openssl rand -base64 48
```

---

## D. 前端 / Web 應用程式

### `VITE_GOOGLE_CLIENT_ID`

| 欄位 | 值 |
|------|-----|
| **用途** | 前端「使用 Google 登入」的 Google OAuth 2.0 用戶端 ID。 |
| **預設值** | `your-google-oauth-client-id.apps.googleusercontent.com` |
| **有效取值** | Google Cloud Console 中有效的 OAuth 2.0 用戶端 ID |
| **安全說明** | 這是公開的用戶端 ID（不是金鑰）。它會被嵌入到編譯後的 JavaScript 套件中。請在 Google Cloud Console 中將授權的 JavaScript 來源限制為您的生產網域。 |
| **常見錯誤** | <ul><li>本地開發時未將 `http://localhost` 新增到授權來源。</li><li>將其與用戶端密鑰混淆（OAuth 2.0 隱式/帶 PKCE 的授權碼流程不需要用戶端密鑰）。</li></ul> |

### `VITE_API_BASE_URL`

| 欄位 | 值 |
|------|-----|
| **用途** | 所有前端 API 呼叫的基礎 URL 前綴。 |
| **預設值** | *（空）* |
| **有效取值** | 空字串、`/api` 或以 `/` 開頭的相對路徑 |
| **安全說明** | **關鍵警告**：此值必須為相對路徑或空。當前端在 Nginx 後執行時，API 呼叫會自動加上前綴並代理到後端。若設為絕對 URL（例如 `http://localhost:8080/api`），瀏覽器將直接連線後端，繞過 Nginx，暴露後端連接埠，破壞 CORS 和單入口安全模型。 |
| **常見錯誤** | <ul><li>本地開發時將 `VITE_API_BASE_URL` 設為 `http://localhost:8080/api`——這會將後端位址洩露給使用者，且在生產環境中會失敗。</li><li>將其設為內部 Docker 主機名稱，如 `http://backend:8080`——瀏覽器無法解析 Docker 服務名稱。</li></ul> |

---

## E. Spring Boot / 後端

### `SPRING_PROFILES_ACTIVE`

| 欄位 | 值 |
|------|-----|
| **用途** | 啟用 Spring Boot 設定檔（Profile）。 |
| **預設值** | `dev` |
| **有效取值** | `dev`、`prod`、`test` |
| **安全說明** | `dev` 設定檔停用 Flyway、使用 24 小時 JWT 過期時間，並啟用詳細錯誤訊息。**絕不要在生產環境中使用 `dev`。** `prod` 設定檔啟用 Flyway 驗證、設定 1 小時 JWT 過期時間，並隱藏 API 回應中的堆疊追蹤。 |
| **常見錯誤** | 生產部署時仍使用 `SPRING_PROFILES_ACTIVE=dev`。 |

### `SPRING_APPLICATION_NAME`

| 欄位 | 值 |
|------|-----|
| **用途** | Spring Boot 應用名稱，顯示在 Actuator、指標和日誌中。 |
| **預設值** | `resume-assistant-backend` |
| **有效取值** | 任意有效的 Spring Boot 應用名稱 |
| **安全說明** | 不是機密。用於可觀測性和服務發現。 |
| **常見錯誤** | 使用空格或特殊字元，破壞 URL 安全識別項。 |

### `SPRING_FLYWAY_ENABLED`

| 欄位 | 值 |
|------|-----|
| **用途** | 是否在啟動時執行 Flyway 資料庫遷移。覆蓋設定檔預設值。 |
| **預設值** | `false`（dev）/ `true`（prod） |
| **有效取值** | `true`、`false` |
| **安全說明** | 在生產環境中停用 Flyway 會阻止架構更新，當程式碼預期新表或新欄時可能導致執行時錯誤。僅在手動管理架構變更時才停用。 |
| **常見錯誤** | <ul><li>在生產環境中設為 `false` 卻忘記手動套用遷移。</li><li>在資料庫已初始化後變更此值，卻不了解對現有資料的影響。</li></ul> |

---

## F. AI 供應商金鑰

您只需設定**一個**供應商。選擇由 `LLM_TEXT_MODEL`、`LLM_VISION_MODEL` 和 `LLM_EMBEDDING_MODEL` 中的前綴決定。

### `GEMINI_API_KEY`

| 欄位 | 值 |
|------|-----|
| **用途** | Google AI Studio 的 API 金鑰（透過 LiteLLM 使用 Gemini 模型）。 |
| **預設值** | `[replace-with-your-gemini-api-key]` |
| **有效取值** | [Google AI Studio](https://aistudio.google.com/app/apikey) 中有效的 Gemini API 金鑰 |
| **安全說明** | Gemini 提供免費額度。免費 tier 不需要信用卡或 GCP 計費。請將此金鑰視為機密——任何獲得它的人都可以消耗您的額度。 |
| **常見錯誤** | <ul><li>將金鑰貼到 `.env` 時帶有周圍空白或引號。</li><li>使用 GCP 服務帳號金鑰代替 AI Studio API 金鑰。</li></ul> |

### `OPENAI_API_KEY`

| 欄位 | 值 |
|------|-----|
| **用途** | 透過 LiteLLM 使用 OpenAI 模型（GPT-4o 等）的 API 金鑰。 |
| **預設值** | *（空）* |
| **有效取值** | 以 `sk-` 開頭的有效 OpenAI API 金鑰 |
| **安全說明** | OpenAI 是付費服務。請設定消費限額並監控使用儀表板，以避免意外帳單。 |
| **常見錯誤** | 同時設定此變數和 `GEMINI_API_KEY`。LiteLLM 將使用與模型前綴匹配的供應商，但保留多個金鑰會增加攻擊面。 |

### `ANTHROPIC_API_KEY`

| 欄位 | 值 |
|------|-----|
| **用途** | 透過 LiteLLM 使用 Anthropic Claude 模型的 API 金鑰。 |
| **預設值** | *（空）* |
| **有效取值** | 有效的 Anthropic API 金鑰 |
| **安全說明** | Claude 模型通常比 Gemini Flash 更昂貴。切換前請評估成本。 |
| **常見錯誤** | 切換供應商時忘記將模型前綴從 `gemini/` 更新為 `anthropic/`。 |

### `GROQ_API_KEY`

| 欄位 | 值 |
|------|-----|
| **用途** | 透過 LiteLLM 使用 Groq（快速推理）模型的 API 金鑰。 |
| **預設值** | *（空）* |
| **有效取值** | 有效的 Groq API 金鑰 |
| **安全說明** | Groq 提供極低延遲，但模型選擇有限。 |
| **常見錯誤** | 期望 Groq 提供嵌入支援——大多數 Groq 模型僅支援文字生成。 |

### `OLLAMA_API_BASE`

| 欄位 | 值 |
|------|-----|
| **用途** | 本地 Ollama 實例的基礎 URL（自托管開源模型）。 |
| **預設值** | `http://localhost:11434` |
| **有效取值** | AI 服務容器可達的任意 HTTP URL |
| **安全說明** | AI 服務容器內的 `localhost:11434` 指的是容器本身，而非 Docker 主機。要連線主機，請使用 `http://host.docker.internal:11434`（Docker Desktop）或主機的區域網路 IP。 |
| **常見錯誤** | <ul><li>保留預設的 `localhost:11434`，然後疑惑 AI 服務為何無法連線。</li><li>啟動技術堆疊前未拉取模型（`ollama pull llama3`）。</li></ul> |

---

## G. 模型參數

### `LLM_TEXT_MODEL`

| 欄位 | 值 |
|------|-----|
| **用途** | 通用文字生成（職缺解析、履歷最佳化、對話）的模型識別項。 |
| **預設值** | `gemini/gemini-2.5-flash` |
| **有效取值** | 任意 LiteLLM 支援的帶供應商前綴的模型字串，例如 `gemini/gemini-2.5-flash`、`openai/gpt-4o-mini`、`vertex_ai/gemini-2.5-flash` |
| **安全說明** | 模型名稱不是機密，但未經預算就切換到更昂貴的模型（例如 `gpt-4o`）可能導致意外成本。 |
| **常見錯誤** | 使用與設定的 API 金鑰不匹配的模型前綴（例如使用 `GEMINI_API_KEY` 時卻用 `openai/` 前綴）。 |

### `LLM_VISION_MODEL`

| 欄位 | 值 |
|------|-----|
| **用途** | 視覺/多模態任務（履歷圖片解析）的模型識別項。 |
| **預設值** | `gemini/gemini-2.5-flash` |
| **有效取值** | 任意支援視覺輸入的 LiteLLM 模型 |
| **安全說明** | 視覺模型通常按 token 計費更貴。請確保所選模型支援圖像輸入。 |
| **常見錯誤** | 為視覺任務選擇純文字模型（例如 `text-embedding-ada-002`）。 |

### `LLM_EMBEDDING_MODEL`

| 欄位 | 值 |
|------|-----|
| **用途** | 生成文字嵌入（向量檢索）的模型識別項。 |
| **預設值** | `gemini/gemini-embedding-001` |
| **有效取值** | 任意 LiteLLM 支援的嵌入模型 |
| **安全說明** | 嵌入模型的輸出維度必須與 `LLM_EMBEDDING_MODEL_DIMENSION` 匹配。 |
| **常見錯誤** | 切換嵌入模型時未更新維度，導致 PostgreSQL 中向量插入失敗。 |

### `LLM_EMBEDDING_MODEL_DIMENSION`

| 欄位 | 值 |
|------|-----|
| **用途** | 所選嵌入模型的輸出維度。必須與模型實際產生的向量大小匹配。 |
| **預設值** | `1536` |
| **有效取值** | 與模型輸出維度匹配的正整數 |
| **安全說明** | 維度不正確會導致執行時 SQL 錯誤（插入 `pgvector` 欄位時失敗），但不會造成安全漏洞。 |
| **常見錯誤** | <ul><li>將 `LLM_EMBEDDING_MODEL` 改為 `openai/text-embedding-3-large`（3072 維）卻保留 `LLM_EMBEDDING_MODEL_DIMENSION=1536`。</li><li>變更維度後忘記重建 PostgreSQL 資料卷——現有向量欄位保留舊維度，將拒絕新插入。</li></ul> |

**維度對照表：**

| 模型 | 維度 |
|------|------|
| `gemini/gemini-embedding-001` | `1536` |
| `openai/text-embedding-ada-002` | `1536` |
| `openai/text-embedding-3-small` | `1536` |
| `openai/text-embedding-3-large` | `3072` |

> **注意**：`.env.example` 中的預設值 `1536` 對應目前預設的 `gemini/gemini-embedding-001` 配置，以及表中列出的 OpenAI 1536 維嵌入模型。

### `LLM_TEMPERATURE`

| 欄位 | 值 |
|------|-----|
| **用途** | 控制 LLM 輸出的隨機性（創造性）。 |
| **預設值** | `0.1` |
| **有效取值** | `0.0` 到 `2.0` |
| **安全說明** | 較低的值產生更確定的輸出，對於結構化 JSON 提取（履歷解析、職缺匹配）更安全。較高的值增加幻覺風險。 |
| **常見錯誤** | 為結構化提取任務設定 `1.0` 或更高，導致無效的 JSON 回應，破壞下游解析器。 |

### `LLM_REQUEST_TIMEOUT_SECONDS`

| 欄位 | 值 |
|------|-----|
| **用途** | 每次 LiteLLM 請求（文字、視覺、嵌入）的最大等待時間。 |
| **預設值** | `60` |
| **有效取值** | 正整數（秒） |
| **安全說明** | 超長的超時可能在重負載下耗盡工作者執行緒。較短的超時提高彈性，但可能導致對慢模型的不必要的重試。 |
| **常見錯誤** | 對於大履歷檔案或批量嵌入作業，將此值設得太低（< 10 秒）。 |

### `LLM_MAX_TOKENS`

| 欄位 | 值 |
|------|-----|
| **用途** | 單次 LLM 請求允許生成的最大輸出 token 數。 |
| **預設值** | `8192` |
| **有效取值** | 正整數 |
| **安全說明** | 較高的值會增加單次請求成本和延遲。但設定過低會導致模型截斷結構化 JSON 回應（如對話回覆、履歷最佳化），從而引發下游 `JSONDecodeError`。 |
| **常見錯誤** | <ul><li>使用提供者預設值（如 1024 或 2048）而未意識到它會截斷長輸出。</li><li>設定過高的值（> 32k）而所選模型不支援，導致提供者報錯。</li><li>未將 AI 服務中的 `json.decoder.JSONDecodeError` 日誌與 `max_tokens` 不足關聯起來。</li></ul> |

### `BACKEND_SERVICE_URL`

| 欄位 | 值 |
|------|-----|
| **用途** | AI 服務呼叫後端 API（例如向量 upsert）時使用的 URL。 |
| **預設值** | `http://backend:8080` |
| **有效取值** | AI 服務容器可達的任意 HTTP URL |
| **安全說明** | 使用 Docker 服務名稱 `backend`，使流量保持在內部網路中。 |
| **常見錯誤** | 在 AI 服務容器內部使用 `http://localhost:8080`。 |

### `BACKEND_QUERY_TIMEOUT`

| 欄位 | 值 |
|------|-----|
| **用途** | AI 服務呼叫後端 API 時的超時秒數。 |
| **預設值** | `5` |
| **有效取值** | 正整數（秒） |
| **安全說明** | 較短的超時可防止 AI 服務在 backend 過載時當機。 |
| **常見錯誤** | 批量向量 upsert 時將此值設得太低，導致部分資料插入。 |

### 增量模型訓練參數

以下參數控制 AI worker 的增量職位訓練閉環。

| 參數 | 預設值 | 說明 |
|------|--------|------|
| `RETRAIN_INTERVAL_HOURS` | `24` | AI worker 檢查緩衝反饋並重新訓練的間隔時間（小時）。 |
| `MIN_SAMPLES_FOR_RETRAIN` | `10` | 觸發重新訓練前所需的最小緩衝反饋樣本數。 |

---

## H. AI 服務日誌

### `LOG_LEVEL`

| 欄位 | 值 |
|------|-----|
| **用途** | 控制 AI 服務日誌記錄器（structlog）的詳細程度。 |
| **預設值** | `INFO` |
| **有效取值** | `DEBUG`、`INFO`、`WARNING`、`ERROR`、`CRITICAL` |
| **安全說明** | `DEBUG` 可能記錄包含履歷內容或使用者資料的請求負載。未經日誌脫敏和保留策略，切勿在生產環境中使用 `DEBUG`。 |
| **常見錯誤** | 生產環境中保留 `DEBUG` 等級，意外將個人識別資訊（PII）記錄到持續性日誌檔案中。 |

---

## I. Vertex AI 設定

### `VERTEX_PROJECT_ID`

| 欄位 | 值 |
|------|-----|
| **用途** | Vertex AI 的 Google Cloud 專案 ID（僅在使用 `vertex_ai/` 模型前綴時需要）。 |
| **預設值** | `ser594-ai-service` |
| **有效取值** | 任意有效的 Google Cloud 專案 ID |
| **安全說明** | 不是機密。僅用於識別計費專案。 |
| **常見錯誤** | 透過 AI Studio 使用 Gemini（`gemini/` 前綴）時設定此值——該模式下此值被忽略。 |

### `VERTEX_LOCATION`

| 欄位 | 值 |
|------|-----|
| **用途** | Vertex AI 模型推理的區域/位置。 |
| **預設值** | `global` |
| **有效取值** | `global`、`us-central1`、`europe-west4`、`asia-northeast1` 等 |
| **安全說明** | 選擇靠近部署位置的區域以最小化延遲，並滿足資料駐留合規要求。 |
| **常見錯誤** | 使用所選模型不可用的區域，導致 `404 Model not found` 錯誤。 |

### `VERTEX_CREDENTIALS`

| 欄位 | 值 |
|------|-----|
| **用途** | Google Cloud 服務帳號 JSON 金鑰檔案的主機絕對路徑。以唯讀卷形式掛載到 AI 服務容器中。 |
| **預設值** | *（空）* |
| **有效取值** | 絕對檔案系統路徑，例如 `/home/user/service-account.json` |
| **安全說明** | **關鍵警告**：此值**必須**為絕對路徑。相對路徑或純檔案名稱（例如 `vertex.json`）會被 Docker/Podman 解釋為命名卷參考，從而靜默建立一個空卷而非掛載您的憑證檔案。該檔案以唯讀方式掛載（`:ro`）以防止意外修改。請將此金鑰儲存在金鑰管理器中並定期輪換。 |
| **常見錯誤** | <ul><li>使用相對路徑，如 `./vertex.json` 或 `~/keys/gcp.json`。</li><li>使用專案目錄內已被 `.gitignore` 的路徑，但忘記將檔案複製到部署主機。</li><li>透過 AI Studio 使用 Gemini（`gemini/` 前綴）時設定此值——AI Studio 模式不需要 ADC 憑證。</li></ul> |

---

## J. 內部 API 金鑰

### `INTERNAL_API_KEY`

| 欄位 | 值 |
|------|-----|
| **用途** | 後端與 AI 服務 REST 端點之間的共用金鑰。保護嵌入生成端點免受未經授權的內網存取。 |
| **預設值** | *（空）* |
| **有效取值** | 任意字串；建議長度 ≥ 32 個字元 |
| **安全說明** | 這是**應用層縱深防禦**金鑰。即使攻擊者攻破某個容器並獲得內部 Docker 網路的存取權限，沒有此金鑰也無法呼叫 LLM 嵌入端點。後端透過 `InternalApiKeyInterceptor` 自動將其附加到每個出站 REST 請求。AI 服務中介軟體在請求標頭缺失或不正確時返回 HTTP 401。 |
| **常見錯誤** | <ul><li>在 `docker-compose.yml` 中為 `backend` 和 `ai-service` 設定不同的值——兩個服務必須共用**完全相同的值**。</li><li>使用短且易被猜中的字串。</li><li>生產環境中忘記設定，因為它在為空時也能正常運作（兩端均跳過驗證）。</li></ul> |

**推薦產生方式：**

```bash
# 32 位元組 = 44 個 base64 字元
openssl rand -base64 32
```

若留空，後端攔截器和 AI 服務中介軟體均跳過檢查。這便於本地開發，但**絕不可**用於生產環境。

## K. JWT 令牌生命週期

### `JWT_ACCESS_EXPIRATION`

| 欄位 | 值 |
|------|-----|
| **用途** | Access Token 過期時間（毫秒）。控制使用者多久需要重新認證。 |
| **預設值** | `86400000`（dev 24 小時）/ `3600000`（prod 1 小時） |
| **有效取值** | 正整數（毫秒） |
| **安全說明** | 較短的值透過限制被竊令牌的利用窗口來提高安全性。較長的值改善使用者體驗但增加風險。生產環境建議 1 小時（`3600000`）。 |
| **常見錯誤** | <ul><li>為方便起見設定極長的值（例如 `31536000000` = 1 年），這違背了令牌輪換的目的。</li><li>忘記 prod 設定檔預設就是 1 小時，即使設定了不同的 dev 值。</li></ul> |

### `JWT_REFRESH_EXPIRATION`

| 欄位 | 值 |
|------|-----|
| **用途** | Refresh Token 過期時間（毫秒）。控制刷新令牌在多長時間內有效以取得新的 Access Token。 |
| **預設值** | `604800000`（7 天） |
| **有效取值** | 正整數（毫秒）；必須大於 `JWT_ACCESS_EXPIRATION` |
| **安全說明** | 刷新令牌按設計是長壽命的。如果刷新令牌洩露，攻擊者可以在整個有效期內維持存取。高安全性部署中考慮更短的值（例如 1–3 天）。 |
| **常見錯誤** | 將 `JWT_REFRESH_EXPIRATION` 設定得比 `JWT_ACCESS_EXPIRATION` 還短，導致令牌到期前無法刷新。 |

---

## L. AI 服務連線

### `AI_SERVICE_BASE_URL`

| 欄位 | 值 |
|------|-----|
| **用途** | 從後端視角看到的 AI 服務 REST API 基礎位址。後端呼叫此端點進行向量生成和職缺匹配。 |
| **預設值** | `http://localhost:8000`（非 Docker）/ `http://ai-service:8000`（Docker） |
| **有效取值** | 後端容器或行程可達的任意 HTTP URL |
| **安全說明** | 在容器網路內部使用 Docker 服務名解析。切勿在未認證的情況下將此端點暴露到公共網際網路。 |
| **常見錯誤** | <ul><li>在 Docker 內執行時設定 `http://localhost:8000`——容器無法將 `localhost` 解析為主機。</li><li>試圖透過 `.env` 覆蓋此值，但 `docker-compose.yml` 已在 `backend` 服務的 `environment` 區塊中硬編碼了該值。需先在 `docker-compose.yml` 中刪除硬編碼行。</li></ul> |

---

## M. 檔案儲存

### `STORAGE_TYPE`

| 欄位 | 值 |
|------|-----|
| **用途** | 決定後端使用哪種儲存後端儲存上傳的履歷檔案。 |
| **預設值** | `minio`（Spring Boot 預設值）/ `local`（Docker Compose 硬編碼） |
| **有效取值** | `minio`、`local`、`s3`、`oss` |
| **安全說明** | `local` 將檔案儲存在 Docker 卷或本機檔案系統中。生產環境中使用多後端副本時，請使用 `minio` 或 `s3`，以便所有實例共享同一個物件儲存。 |
| **常見錯誤** | <ul><li>在未配置 MinIO 端點和憑證的情況下設定 `STORAGE_TYPE=minio`。</li><li>在多副本部署中使用 `local`，導致上傳到一個副本的檔案對其他副本不可見。</li></ul> |

### MinIO 設定（`STORAGE_TYPE=minio` 時生效）

### `MINIO_ENDPOINT`

| 欄位 | 值 |
|------|-----|
| **用途** | MinIO 伺服器位址。 |
| **預設值** | `http://localhost:9000`（dev）/ `http://minio:9000`（Docker） |
| **有效取值** | 任意 HTTP(S) URL |
| **安全說明** | 生產環境請使用 HTTPS。驗證 TLS 憑證或配置後端信任庫。 |
| **常見錯誤** | 在 Docker 內使用 `http://localhost:9000` 而不是服務名稱。 |

### `MINIO_ACCESS_KEY`

| 欄位 | 值 |
|------|-----|
| **用途** | MinIO 存取金鑰（使用者名稱）。 |
| **預設值** | `minioadmin` |
| **有效取值** | 任意字串 |
| **安全說明** | 生產環境請立即從預設的 `minioadmin` 更換。 |
| **常見錯誤** | 在開發和生產 MinIO 實例之間重複使用相同的憑證。 |

### `MINIO_SECRET_KEY`

| 欄位 | 值 |
|------|-----|
| **用途** | MinIO 秘密金鑰（密碼）。 |
| **預設值** | `minioadmin` |
| **有效取值** | 任意字串；建議長度 ≥ 16 |
| **安全說明** | 儲存在金鑰管理器中。洩露的金鑰將授予對所有儲存桶的完全讀寫權限。 |
| **常見錯誤** | 將金鑰提交到 Git 或在聊天訊息中共享。 |

### `MINIO_BUCKET_NAME`

| 欄位 | 值 |
|------|-----|
| **用途** | 用於履歷檔案的 MinIO 儲存桶名稱。 |
| **預設值** | `resumes` |
| **有效取值** | 任意有效的 S3 儲存桶名稱 |
| **安全說明** | 儲存桶名稱在 S3 中是全域唯一的，但在 MinIO 中僅是本機唯一。 |
| **常見錯誤** | 使用大寫字母或底線，這在與 DNS 相容的 S3 儲存桶名稱中是無效的。 |

### `MINIO_CONNECT_TIMEOUT`

| 欄位 | 值 |
|------|-----|
| **用途** | 連線 MinIO 的 TCP 逾時時間（毫秒）。 |
| **預設值** | `5000` |
| **有效取值** | 正整數（毫秒） |
| **安全說明** | 較短的逾時可防止在無法連線的端點上當機，但在慢速網路上可能會失敗。 |
| **常見錯誤** | 在高延遲網路上設定得太低（< 1000 毫秒）。 |

### `MINIO_WRITE_TIMEOUT`

| 欄位 | 值 |
|------|-----|
| **用途** | 向 MinIO 寫入資料的 Socket 逾時時間（毫秒）。 |
| **預設值** | `60000` |
| **有效取值** | 正整數（毫秒） |
| **安全說明** | 大檔案上傳需要足夠的逾時時間。 |
| **常見錯誤** | 對於多 MB 的履歷 PDF 上傳設定得太低。 |

### `MINIO_READ_TIMEOUT`

| 欄位 | 值 |
|------|-----|
| **用途** | 從 MinIO 讀取資料的 Socket 逾時時間（毫秒）。 |
| **預設值** | `30000` |
| **有效取值** | 正整數（毫秒） |
| **安全說明** | 下載大檔案需要足夠的逾時時間。 |
| **常見錯誤** | 與寫入逾時相同——對大檔案不足。 |

### 本地儲存設定（`STORAGE_TYPE=local` 時生效）

### `LOCAL_STORAGE_BASE_PATH`

| 欄位 | 值 |
|------|-----|
| **用途** | 本機檔案儲存的根目錄。 |
| **預設值** | `./uploads`（dev）/ `/app/uploads`（Docker） |
| **有效取值** | 任意絕對或相對檔案系統路徑 |
| **安全說明** | 確保目錄對後端行程使用者可寫入。在 Docker 中，使用掛載卷以持久化資料。 |
| **常見錯誤** | 使用相對路徑，導致根據工作目錄不同解析到不同位置。 |

### `LOCAL_STORAGE_RESUME_PATH`

| 欄位 | 值 |
|------|-----|
| **用途** | `LOCAL_STORAGE_BASE_PATH` 下存放履歷檔案的子目錄。 |
| **預設值** | `resumes` |
| **有效取值** | 任意有效的目錄名稱 |
| **安全說明** | 無特殊要求。 |
| **常見錯誤** | 使用路徑分隔符（例如 `resumes/2024`）——這由 `LOCAL_STORAGE_DATE_SUBDIR` 處理。 |

### `LOCAL_STORAGE_DATE_SUBDIR`

| 欄位 | 值 |
|------|-----|
| **用途** | 是否為上傳的檔案建立基於日期的子目錄（例如 `2025/05/06/`）。 |
| **預設值** | `true` |
| **有效取值** | `true`、`false` |
| **安全說明** | 日期子目錄使透過掃描單一目錄來列舉所有檔案變得更加困難。 |
| **常見錯誤** | 設為 `false` 並將數千個檔案累積在一個資料夾中，降低檔案系統效能。 |

### `LOCAL_STORAGE_URL_PREFIX`

| 欄位 | 值 |
|------|-----|
| **用途** | 生成檔案存取 URL 時新增的 URL 前綴。適用於透過 Nginx 反向代理提供檔案服務。 |
| **預設值** | *（空）* |
| **有效取值** | 任意 URL 路徑前綴，例如 `/files`、`https://cdn.example.com` |
| **安全說明** | 如果為空，後端生成相對 URL。設定外部 CDN 可提高效能，但需要適當的存取控制。 |
| **常見錯誤** | 新增尾部斜線導致 URL 中出現雙斜線。 |

### AI 模型產物儲存 (MinIO)

這些變數由 Python AI 服務和 AI worker 使用，用於保存與載入增量匹配模型 artifact。它們與上方後端履歷檔案儲存設定是分開的。

### `MINIO_ENDPOINT`

| 欄位 | 值 |
|------|-----|
| **用途** | AI 模型 registry 使用的 MinIO 伺服器地址。 |
| **預設值** | `http://minio:9000` |
| **有效取值** | 任意 HTTP(S) URL |
| **安全說明** | 生產環境請使用 HTTPS。請驗證 TLS 憑證或設定信任庫。 |
| **常見錯誤** | 在 Docker 或 Kubernetes 內使用 `http://localhost:9000`，而不是服務名稱。 |

### `MINIO_ACCESS_KEY`

| 欄位 | 值 |
|------|-----|
| **用途** | AI 模型 registry 使用的 MinIO access key。 |
| **預設值** | `minioadmin` |
| **有效取值** | 任意字串 |
| **安全說明** | 非本機開發環境請立即更換預設值。 |
| **常見錯誤** | 開發與生產 MinIO 實例重複使用相同憑證。 |

### `MINIO_SECRET_KEY`

| 欄位 | 值 |
|------|-----|
| **用途** | AI 模型 registry 使用的 MinIO secret key。 |
| **預設值** | `minioadmin` |
| **有效取值** | 任意字串；建議長度 ≥ 16 |
| **安全說明** | 請存放於 secret manager。密鑰外洩會授予模型 artifact 的讀寫權限。 |
| **常見錯誤** | 將密鑰提交到 Git，或在聊天紀錄中分享。 |

### `MINIO_MODEL_BUCKET`

| 欄位 | 值 |
|------|-----|
| **用途** | 用於保存 `ranker_model_<version>.txt` 和 `latest_meta.json` 等模型 artifact 的 bucket 名稱。 |
| **預設值** | `ai-models` |
| **有效取值** | 任意有效的 S3 bucket 名稱 |
| **安全說明** | 模型 artifact 可能編碼聚合後的使用者/職位匹配行為，應保持私有。 |
| **常見錯誤** | 以為 `AWS_S3_*` 變數會控制模型 artifact 儲存。AI 服務使用 `MINIO_*` 端點、憑證與模型 bucket 變數。 |

### `RETRAIN_INTERVAL_HOURS`

| 欄位 | 值 |
|------|-----|
| **用途** | AI worker 檢查新反饋並重新訓練 LightGBM 模型的間隔時間（小時）。 |
| **預設值** | `24` |
| **有效取值** | 正整數 |
| **安全說明** | 頻繁的重新訓練會消耗更多的 CPU 和記憶體資源。 |
| **常見錯誤** | 在沒有足夠新資料的情況下將此值設定得太低（例如 `1`），導致不必要的重新訓練循環。 |

### AWS S3 設定（`STORAGE_TYPE=s3` 時生效）

### `AWS_S3_REGION`

| 欄位 | 值 |
|------|-----|
| **用途** | S3 操作的 AWS 區域。 |
| **預設值** | `us-east-1` |
| **有效取值** | 任意有效的 AWS 區域代碼 |
| **安全說明** | 選擇靠近部署位置的區域以最小化延遲並滿足資料駐留合規要求。 |
| **常見錯誤** | 使用儲存桶不存在的區域，導致 `NoSuchBucket` 錯誤。 |

### `AWS_S3_ENDPOINT`

| 欄位 | 值 |
|------|-----|
| **用途** | 相容 S3 的第三方儲存自訂端點（MinIO、Ceph、Wasabi 等）。 |
| **預設值** | *（空）* |
| **有效取值** | 任意 HTTP(S) URL 或空 |
| **安全說明** | 對於官方 AWS S3 請留空。第三方 S3 相容服務需要設定此項。 |
| **常見錯誤** | 在真正的 AWS S3 上設定此項，覆蓋官方端點並導致簽名錯誤。 |

### `AWS_S3_ACCESS_KEY`

| 欄位 | 值 |
|------|-----|
| **用途** | AWS IAM 存取金鑰或 S3 相容存取金鑰。 |
| **預設值** | *（空）* |
| **有效取值** | 任意字串 |
| **安全說明** | 在 AWS EC2/EKS 上執行時，優先使用 IAM 角色（執行個體設定檔）而非長期存取金鑰。 |
| **常見錯誤** | 使用根帳戶金鑰而非具有有限權限的 IAM 使用者金鑰。 |

### `AWS_S3_SECRET_KEY`

| 欄位 | 值 |
|------|-----|
| **用途** | AWS IAM 秘密金鑰或 S3 相容秘密金鑰。 |
| **預設值** | *（空）* |
| **有效取值** | 任意字串 |
| **安全說明** | 與存取金鑰相同——優先使用 IAM 角色。切勿提交到版本控制。 |
| **常見錯誤** | 貼上金鑰時帶有周圍空白或引號。 |

### `AWS_S3_BUCKET_NAME`

| 欄位 | 值 |
|------|-----|
| **用途** | 用於履歷檔案的 S3 儲存桶名稱。 |
| **預設值** | *（空）* |
| **有效取值** | 任意有效的 S3 儲存桶名稱 |
| **安全說明** | 確保儲存桶策略限制公共存取。啟用伺服器端加密。 |
| **常見錯誤** | 使用在全域 S3 命名空間中已被佔用的儲存桶名稱。 |

### `AWS_S3_PATH_STYLE`

| 欄位 | 值 |
|------|-----|
| **用途** | 是否使用路徑風格儲存桶 URL（`s3.example.com/bucketname`）而非虛擬託管風格（`bucketname.s3.example.com`）。 |
| **預設值** | `false` |
| **有效取值** | `true`、`false` |
| **安全說明** | 某些第三方 S3 相容服務（例如不帶 DNS 的 MinIO）需要路徑風格。AWS S3 推薦使用虛擬託管風格。 |
| **常見錯誤** | 在使用基於 IP 的端點的 MinIO 上保留 `false`，導致 DNS 解析失敗。 |

### `AWS_S3_CONNECTION_TIMEOUT`

| 欄位 | 值 |
|------|-----|
| **用途** | S3 連線逾時時間（毫秒）。 |
| **預設值** | `5000` |
| **有效取值** | 正整數（毫秒） |
| **安全說明** | 較短的逾時可提高對無法連線端點的恢復能力。 |
| **常見錯誤** | 對於高延遲區域設定得太低。 |

### `AWS_S3_SOCKET_TIMEOUT`

| 欄位 | 值 |
|------|-----|
| **用途** | S3 Socket 逾時時間（毫秒）。 |
| **預設值** | `50000` |
| **有效取值** | 正整數（毫秒） |
| **安全說明** | 大檔案傳輸需要足夠的逾時時間。 |
| **常見錯誤** | 與 MinIO 讀/寫逾時相同。 |

### `AWS_S3_MAX_CONNECTIONS`

| 欄位 | 值 |
|------|-----|
| **用途** | 到 S3 的最大並發 HTTP 連線數。 |
| **預設值** | `50` |
| **有效取值** | 正整數 |
| **安全說明** | 較高的值可提高大量上傳的吞吐量，但消耗更多記憶體。 |
| **常見錯誤** | 在未監控的情況下設定極高的值（> 500），導致記憶體壓力。 |

### `AWS_S3_ENCRYPTION_ENABLED`

| 欄位 | 值 |
|------|-----|
| **用途** | 是否為上傳的物件啟用伺服器端加密。 |
| **預設值** | `false` |
| **有效取值** | `true`、`false` |
| **安全說明** | 在生產環境中啟用此項以滿足合規要求（GDPR、HIPAA 等）。 |
| **常見錯誤** | 在未指定 KMS 金鑰的情況下啟用加密，可能使用具有不同權限的預設 AWS 託管金鑰。 |

### `AWS_S3_KMS_KEY_ID`

| 欄位 | 值 |
|------|-----|
| **用途** | 伺服器端加密的 AWS KMS 金鑰 ID 或 ARN。僅在 `AWS_S3_ENCRYPTION_ENABLED=true` 時使用。 |
| **預設值** | *（空）* |
| **有效取值** | KMS 金鑰 ID、金鑰 ARN 或別名 ARN |
| **安全說明** | 使用客戶託管金鑰（CMK）可提供稽核追蹤和細粒度存取控制。 |
| **常見錯誤** | 指定來自不同區域的金鑰，導致跨區域 KMS 呼叫和更高的延遲。 |

### 阿里雲 OSS 設定（`STORAGE_TYPE=oss` 時生效）

### `ALIYUN_OSS_ENDPOINT`

| 欄位 | 值 |
|------|-----|
| **用途** | OSS 端點 URL。 |
| **預設值** | *（空）* |
| **有效取值** | 任意有效的 OSS 端點，例如 `https://oss-cn-hangzhou.aliyuncs.com` |
| **安全說明** | 當後端和 OSS 位於同一阿里雲區域時，請使用內網端點（`oss-cn-hangzhou-internal.aliyuncs.com`）以避免公網流量費用。 |
| **常見錯誤** | 在有內網端點可用的情況下使用公網端點，產生不必要的出站流量費用。 |

### `ALIYUN_OSS_ACCESS_KEY_ID`

| 欄位 | 值 |
|------|-----|
| **用途** | 阿里雲 AccessKey ID。 |
| **預設值** | *（空）* |
| **有效取值** | 任意有效的阿里雲 AccessKey ID |
| **安全說明** | 在阿里雲 ECS 上執行時，優先使用 RAM 角色（執行個體角色）而非長期 AccessKey。 |
| **常見錯誤** | 使用根帳戶 AccessKey 而非具有有限權限的 RAM 使用者金鑰。 |

### `ALIYUN_OSS_ACCESS_KEY_SECRET`

| 欄位 | 值 |
|------|-----|
| **用途** | 阿里雲 AccessKey 密碼。 |
| **預設值** | *（空）* |
| **有效取值** | 任意字串 |
| **安全說明** | 與 AccessKey ID 相同——優先使用 RAM 角色。 |
| **常見錯誤** | 將金鑰提交到版本控制。 |

### `ALIYUN_OSS_BUCKET_NAME`

| 欄位 | 值 |
|------|-----|
| **用途** | 用於履歷檔案的 OSS 儲存桶名稱。 |
| **預設值** | *（空）* |
| **有效取值** | 任意有效的 OSS 儲存桶名稱 |
| **安全說明** | 確保儲存桶 ACL 和策略限制公共存取。 |
| **常見錯誤** | 使用與端點不同區域的儲存桶，導致跨區域延遲。 |

### `ALIYUN_OSS_CDN_DOMAIN`

| 欄位 | 值 |
|------|-----|
| **用途** | 用於生成公共檔案 URL 的自訂 CDN 網域名稱。 |
| **預設值** | *（空）* |
| **有效取值** | 任意有效網域，例如 `https://cdn.example.com` |
| **安全說明** | CDN 網域應具有適當的存取控制和 HTTPS。 |
| **常見錯誤** | 設定網域名稱後忘記配置 CDN 回源設定。 |

### `ALIYUN_OSS_CONNECTION_TIMEOUT`

| 欄位 | 值 |
|------|-----|
| **用途** | OSS 連線逾時時間（毫秒）。 |
| **預設值** | `5000` |
| **有效取值** | 正整數（毫秒） |
| **安全說明** | 與 S3 連線逾時相同。 |
| **常見錯誤** | 與 S3 相同。 |

### `ALIYUN_OSS_SOCKET_TIMEOUT`

| 欄位 | 值 |
|------|-----|
| **用途** | OSS Socket 逾時時間（毫秒）。 |
| **預設值** | `50000` |
| **有效取值** | 正整數（毫秒） |
| **安全說明** | 與 S3 Socket 逾時相同。 |
| **常見錯誤** | 與 S3 相同。 |

### `ALIYUN_OSS_MAX_CONNECTIONS`

| 欄位 | 值 |
|------|-----|
| **用途** | 到 OSS 的最大並發連線數。 |
| **預設值** | `50` |
| **有效取值** | 正整數 |
| **安全說明** | 與 S3 最大連線數相同。 |
| **常見錯誤** | 與 S3 相同。 |

### `ALIYUN_OSS_DOWNLOAD_BUFFER`

| 欄位 | 值 |
|------|-----|
| **用途** | OSS 下載緩衝區大小（KB）。 |
| **預設值** | `64` |
| **有效取值** | 正整數（KB） |
| **安全說明** | 較大的緩衝區可提高下載速度，但增加記憶體使用。 |
| **常見錯誤** | 為小檔案設定極大的值（> 1024 KB），浪費記憶體。 |

### `ALIYUN_OSS_UPLOAD_PART_SIZE`

| 欄位 | 值 |
|------|-----|
| **用途** | 分片上傳的片大小（MB）。 |
| **預設值** | `5` |
| **有效取值** | 正整數（MB） |
| **安全說明** | 大於 5 GB 的檔案必須使用分片上傳。較小的分片可提高斷點續傳能力，但增加 API 呼叫開銷。 |
| **常見錯誤** | 將分片大小設定為低於 OSS 最小值（1 MB）或高於最大值（5 GB）。 |

### `ALIYUN_OSS_SSE`

| 欄位 | 值 |
|------|-----|
| **用途** | OSS 物件的伺服器端加密演算法。 |
| **預設值** | *（空）* |
| **有效取值** | `AES256`、`KMS`、`SM4` 或空 |
| **安全說明** | 為合規啟用加密。`AES256` 是最簡單的選項。`KMS` 提供稽核追蹤。 |
| **常見錯誤** | 在未配置 KMS 金鑰 ID 的情況下設定 `KMS`。 |

### `ALIYUN_OSS_KMS_KEY_ID`

| 欄位 | 值 |
|------|-----|
| **用途** | OSS 加密的 KMS 金鑰 ID。僅在 `ALIYUN_OSS_SSE=KMS` 時使用。 |
| **預設值** | *（空）* |
| **有效取值** | 任意有效的阿里雲 KMS 金鑰 ID |
| **安全說明** | 使用客戶託管金鑰比預設服務金鑰提供更好的存取控制。 |
| **常見錯誤** | 指定來自不同區域的金鑰，或沒有適當的 RAM 權限。 |

---

## O. 郵箱驗證設定

### `EMAIL_VERIFICATION_ENABLED`

| 欄位 | 值 |
|------|-----|
| **用途** | 使用者註冊時郵箱驗證的總開關。開啟後，新使用者必須輸入郵件收到的6位驗證碼才能完成註冊。 |
| **預設值** | `false` |
| **有效取值** | `true`、`false` |
| **安全說明** | 即使設定了SMTP憑證，也只有在該標誌為 `true` 時才會強制驗證。這確保了向後相容和安全的漸進式上線。 |
| **常見錯誤** | 設為 `true` 但未設定 `SMTP_HOST`、`SMTP_USERNAME` 和 `SMTP_PASSWORD`，導致所有驗證請求在執行時失敗。 |

### `EMAIL_FROM`

| 欄位 | 值 |
|------|-----|
| **用途** | 驗證郵件中顯示的寄件人地址。 |
| **預設值** | `noreply@resume-assistant.local` |
| **有效取值** | 任意有效的郵箱地址 |
| **安全說明** | 某些SMTP服務商要求 `From` 地址在其主控台中已驗證或已註冊。使用未驗證的地址可能導致郵件被拒收或進入垃圾郵件匣。 |
| **常見錯誤** | 使用個人Gmail地址但未啟用「應用程式密碼」，或未為網域設定SPF/DKIM記錄。 |

### `SMTP_HOST`

| 欄位 | 值 |
|------|-----|
| **用途** | 用於傳送驗證郵件的SMTP中繼伺服器主機名稱。 |
| **預設值** | *（空）* |
| **有效取值** | 任何可達的SMTP主機名稱（例如 `smtp.gmail.com`、`smtp.office365.com`、`smtp.mailgun.org`） |
| **安全說明** | 優先選擇強制使用TLS的SMTP提供者（587連接埠 + STARTTLS）。避免使用25連接埠的明文SMTP。 |
| **常見錯誤** | 在容器內使用 `localhost`；容器不共用主機的回送介面。如需使用本機中繼，請使用Docker主機閘道位址：`host.docker.internal`。 |

### `SMTP_PORT`

| 欄位 | 值 |
|------|-----|
| **用途** | SMTP中繼的TCP連接埠。 |
| **預設值** | `587` |
| **有效取值** | `25`、`465`（SSL）、`587`（STARTTLS）、`2525`（備選） |
| **安全說明** | 587連接埠配合STARTTLS是現代標準。465連接埠是舊版SSL連接埠。25連接埠通常被雲端服務商封鎖。 |
| **常見錯誤** | 對STARTTLS使用465連接埠，或對隱式SSL使用587連接埠；加密方式與連接埠不相符會導致連線停滯。 |

### `SMTP_USERNAME`

| 欄位 | 值 |
|------|-----|
| **用途** | SMTP認證的使用者名稱，通常完整的郵箱地址。 |
| **預設值** | *（空）* |
| **有效取值** | SMTP伺服器接受的任意字串 |
| **安全說明** | 將此視為敏感設定。不要將真實憑證提交到版本控制。 |
| **常見錯誤** | 對於Gmail，使用一般Google密碼而非應用程式密碼（開啟兩步驟驗證後必須使用應用程式密碼）。 |

### `SMTP_PASSWORD`

| 欄位 | 值 |
|------|-----|
| **用途** | SMTP認證的密碼或應用程式密碼。 |
| **預設值** | *（空）* |
| **有效取值** | 任意字串 |
| **安全說明** | 僅儲存在 `.env` 中。定期輪替。如果憑證洩露，攻擊者可以冒用您的身分傳送郵件。 |
| **常見錯誤** | 將 `.env` 提交到Git。使用弱密碼或重複使用的密碼。團隊成員離職後未輪替憑證。 |

### `EMAIL_CODE_EXPIRY`

| 欄位 | 值 |
|------|-----|
| **用途** | 驗證碼過期前的有效時間（秒）。 |
| **預設值** | `300`（5分鐘） |
| **有效取值** | 正整數，建議範圍 `60–900` |
| **安全說明** | 更短的過期時間可減少暴力破解視窗。過短（< 60秒）則影響使用者體驗。 |
| **常見錯誤** | 設定過長的過期時間（例如1小時），使暴力破解攻擊更容易成功。 |

### `EMAIL_RESEND_COOLDOWN`

| 欄位 | 值 |
|------|-----|
| **用途** | 同一郵箱重新取得驗證碼前必須等待的最短秒數。 |
| **預設值** | `60`（1分鐘） |
| **有效取值** | 正整數，建議範圍 `30–300` |
| **安全說明** | 防止高頻重發濫用。與已註冊郵箱直接拒絕發件機制搭配使用。 |
| **常見錯誤** | 設為 `0` 會失去對誤觸雙擊和惡意濫用的防護。 |

### `EMAIL_MAX_ATTEMPTS`

| 欄位 | 值 |
|------|-----|
| **用途** | 驗證碼校驗失敗的最大次數，超過則作廢該驗證碼。 |
| **預設值** | `3` |
| **有效取值** | 正整數，建議範圍 `3–5` |
| **安全說明** | 限制暴力猜測。超過閾值後，使用者必須重新取得驗證碼。 |
| **常見錯誤** | 設為較大值（例如10），在5分鐘有效期內使6位數字驗證碼更容易被暴力破解。 |

## R. Redis / 共享狀態層

Redis 作為共享狀態層，為分散式快取、分散式鎖（ShedLock）、去重集合和 Pub/Sub 跨實例訊息提供支援。它**不替代** RabbitMQ；MQ 負責通訊，Redis 負責狀態。

### `REDIS_HOST`

| 欄位 | 值 |
|------|-----|
| **用途** | 後端和 AI 服務連線 Redis 時使用的主機名。 |
| **預設值** | `redis`（Docker 服務名稱）/ `localhost`（非 Docker） |
| **有效取值** | Docker 服務名稱、容器 IP 或外部主機名 |
| **安全說明** | 使用 Docker 服務名稱（`redis`）可確保流量停留在內部橋接網路上。 |
| **常見錯誤** | 在容器內部使用 `localhost`。 |

### `REDIS_PORT`

| 欄位 | 值 |
|------|-----|
| **用途** | Redis 監聽連接埠。 |
| **預設值** | `6379` |
| **有效取值** | `1024–65535` |
| **安全說明** | 網路隔離是主要防禦手段。更改連接埠帶來的收益極小。 |
| **常見錯誤** | 更改連接埠後未同步更新 Redis 容器命令/配置以及後端、AI 服務環境變數。Docker Compose 預設不向主機暴露 Redis。 |

### `REDIS_PASSWORD`

| 欄位 | 值 |
|------|-----|
| **用途** | Redis AUTH 密碼。 |
| **預設值** | *（空）* |
| **有效取值** | 任意字串；留空表示無認證（開發環境預設值） |
| **安全說明** | 生產環境中應設定強密碼並配置 Redis 的 `requirepass`。即使具備網路隔離，密碼認證仍是縱深防禦的必備層。 |
| **常見錯誤** | 在生產部署中留空密碼。 |

---

## P. 滑動驗證碼配置

### `CAPTCHA_ENABLED`

| 欄位 | 值 |
|------|-----|
| **用途** | 滑動驗證碼總開關。開啟後，註冊和登入介面要求完成驗證碼校驗。 |
| **預設值** | `true` |
| **有效取值** | `true`、`false` |
| **安全說明** | 關閉驗證碼會移除一層機器人防護。僅在受信任的內部網路或自動化測試環境中設為 `false`。 |
| **常見錯誤** | 在面向公眾的實例上設為 `false`，且未配置替代防機器人措施（例如速率限制或 WAF 規則）。 |

### `CAPTCHA_TOLERANCE`

| 欄位 | 值 |
|------|-----|
| **用途** | 校驗滑塊位置時的像素容差。數值越大，拼圖越容易通過。 |
| **預設值** | `8` |
| **有效取值** | 非負整數，建議範圍 `4–20` |
| **安全說明** | 容差過大（例如 `50`）會實質性地失效挑戰，因為任何落點都會被接受。 |
| **常見錯誤** | 設為 `0` 要求像素級對齊，導致正常使用者反覆失敗並放棄註冊。 |

### `CAPTCHA_TOKEN_EXPIRY`

| 欄位 | 值 |
|------|-----|
| **用途** | captchaToken 過期前的有效時間（秒）。 |
| **預設值** | `300`（5分鐘） |
| **有效取值** | 正整數，建議範圍 `60–600` |
| **安全說明** | 更短的過期時間可減少重放攻擊視窗。過短（< 30秒）則在慢速網路下影響使用者體驗。 |
| **常見錯誤** | 設定過長的過期時間（例如1小時），使攻擊者可以復用一次已解決的挑戰發起多次請求。 |

### `CAPTCHA_TRACK_WIDTH`

| 欄位 | 值 |
|------|-----|
| **用途** | 滑軌寬度（像素）。決定挑戰的難度範圍。 |
| **預設值** | `300` |
| **有效取值** | 正整數，建議範圍 `200–500` |
| **安全說明** | 過窄的滑軌（< 150 px）更容易被機器人暴力破解，因為搜尋空間更小。 |
| **常見錯誤** | 設定的寬度與前端 CSS 不匹配，導致視覺錯位和落點失敗。 |

### `CAPTCHA_MAX_ATTEMPTS`

| 欄位 | 值 |
|------|-----|
| **用途** | 單條 challenge 的最大驗證次數，超過則作廢該 captchaToken。 |
| **預設值** | `5` |
| **有效取值** | 正整數，建議範圍 `3–10` |
| **安全說明** | 限制對滑塊位置的暴力猜測。超過閾值後，使用者必須重新取得挑戰。 |
| **常見錯誤** | 設為較大值（例如50），在 token 有效期內允許無限制猜測，使挑戰失去意義。 |

---

## N. 後端日誌

### `LOG_HIBERNATE_LEVEL`

| 欄位 | 值 |
|------|-----|
| **用途** | Hibernate SQL 陳述式和 JDBC 操作的日誌層級。 |
| **預設值** | `DEBUG`（dev）/ `WARN`（prod） |
| **有效取值** | `TRACE`、`DEBUG`、`INFO`、`WARN`、`ERROR`、`OFF` |
| **安全說明** | `DEBUG` 和 `TRACE` 可能記錄包含履歷中個人資料的繫結參數值。未經日誌脫敏，切勿在生產環境中使用 `DEBUG`。 |
| **常見錯誤** | 在生產環境中保留 `DEBUG`，意外將個人識別資訊（PII）記錄到持續性檔案中。 |

### `LOG_APP_LEVEL`

| 欄位 | 值 |
|------|-----|
| **用途** | 應用程式碼（`edu.asu.ser594.resumeassistant`）的日誌層級。 |
| **預設值** | `DEBUG`（dev）/ `INFO`（prod） |
| **有效取值** | `TRACE`、`DEBUG`、`INFO`、`WARN`、`ERROR`、`OFF` |
| **安全說明** | `DEBUG` 會列印請求詳情、服務呼叫和例外堆疊追蹤。對故障排除很有用，但會生成大量日誌。 |
| **常見錯誤** | 在未配置日誌輪替的情況下設定 `TRACE`，導致繁忙伺服器在數小時內耗盡磁碟空間。 |

---

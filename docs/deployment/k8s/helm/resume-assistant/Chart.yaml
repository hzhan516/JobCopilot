apiVersion: v2
name: resume-assistant
description: |
  Production-grade Helm chart for Resume Assistant (智能求职助手).
  Supports both embedded middleware (PostgreSQL + RabbitMQ + Redis inside K8s)
  and managed middleware (cloud-managed database / MQ / cache).
type: application
version: 1.0.0
appVersion: "1.0.0"
keywords:
  - resume
  - job-search
  - ai
  - spring-boot
  - fastapi
  - pgvector
home: https://github.com/your-org/resume-assistant
sources:
  - https://github.com/your-org/resume-assistant
maintainers:
  - name: SER594 Team
    email: ser594-team@asu.edu

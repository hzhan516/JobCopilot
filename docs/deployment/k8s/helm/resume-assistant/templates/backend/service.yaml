{{- if .Values.backend.enabled }}
# =============================================================================
# Backend Service
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: {{ include "resume-assistant.fullname" . }}-backend
  namespace: {{ include "resume-assistant.namespace" . }}
  labels:
    {{- include "resume-assistant.backend.labels" . | nindent 4 }}
  {{- if .Values.observability.prometheus.serviceMonitor.enabled }}
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8080"
    prometheus.io/path: "/api/actuator/prometheus"
  {{- end }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: backend
    {{- include "resume-assistant.selectorLabels" . | nindent 4 }}
{{- end }}

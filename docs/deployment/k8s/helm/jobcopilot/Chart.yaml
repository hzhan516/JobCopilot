apiVersion: v2
name: jobcopilot
description: |
  Production-grade Helm chart for JobCopilot.
  Supports both embedded middleware (PostgreSQL + RabbitMQ + Redis inside K8s)
  and managed middleware (cloud-managed database / MQ / cache).
type: application
version: 1.0.0
appVersion: "1.0.0"
keywords:
  - resume
  - job-search
  - ai
  - spring-boot
  - fastapi
  - pgvector
home: https://github.com/jobcopilot/jobcopilot
sources:
  - https://github.com/jobcopilot/jobcopilot
maintainers:
  - name: JobCopilot Labs
    email: team@jobcopilot.io

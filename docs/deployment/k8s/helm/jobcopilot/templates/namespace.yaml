{{- if .Values.namespace.create }}
# =============================================================================
# Namespace
# =============================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "jobcopilot.namespace" . }}
  labels:
    {{- include "jobcopilot.labels" . | nindent 4 }}
  {{- end }}
# =============================================================================
# AI Service
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: resume-assistant-ai-service
  namespace: resume-assistant
  labels:
    app.kubernetes.io/name: resume-assistant
    app.kubernetes.io/component: ai-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: resume-assistant
    app.kubernetes.io/component: ai-service

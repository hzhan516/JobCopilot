# =============================================================================
# Secrets Placeholder / Secret 占位符
# =============================================================================
# IMPORTANT: Do NOT commit real secrets to version control.
# 重要：请勿将真实密钥提交到版本控制。
#
# Create the actual Secret using one of these methods:
# 使用以下方法之一创建真实 Secret：
#
#   1. Use generate-secrets.sh: / 使用 generate-secrets.sh：
#      ./scripts/generate-secrets.sh .env jobcopilot jobcopilot-secrets | kubectl apply -f -
#
#   2. Create manually: / 手动创建：
#      kubectl create secret generic jobcopilot-secrets \
#        --namespace=jobcopilot \
#        --from-literal=JWT_SECRET=your-secret \
#        --from-literal=POSTGRES_PASSWORD=your-password \
#        ...
#
#   3. Use External Secrets Operator / 使用 External Secrets Operator：
#      https://external-secrets.io/
#
# This file is left empty intentionally to prevent accidental secret commits.
# 本文件故意留空，以防止意外提交密钥。
